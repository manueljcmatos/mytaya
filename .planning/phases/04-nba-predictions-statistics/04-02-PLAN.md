---
phase: 04-nba-predictions-statistics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/predictions.ts
  - src/components/predictions/PredictionList.tsx
  - src/components/predictions/PredictionCard.astro
  - src/components/predictions/PredictionDetail.tsx
  - src/pages/en/predictions/[slug].astro
  - src/pages/tl/hula/[slug].astro
  - src/i18n/ui.ts
autonomous: true
requirements: [BASK-02]

must_haves:
  truths:
    - "Predictions listing shows both football and basketball mixed by default, sorted by match time"
    - "User can filter predictions by sport using tabs (All Sports, Football, Basketball)"
    - "NBA prediction cards display conference badge (Eastern/Western) and spread/total info"
    - "Detail pages render NBA predictions with spread, moneyline, and over/under context"
  artifacts:
    - path: "src/lib/predictions.ts"
      provides: "Sport-parameterized prediction queries"
      exports: ["getTodayPredictions", "getPastPredictions", "getLeagues"]
    - path: "src/components/predictions/PredictionList.tsx"
      provides: "Sport tabs (All/Football/Basketball) in prediction listing"
      contains: "sport"
    - path: "src/components/predictions/PredictionDetail.tsx"
      provides: "NBA prediction detail with spread/total display"
      contains: "spread"
  key_links:
    - from: "src/components/predictions/PredictionList.tsx"
      to: "Supabase predictions table"
      via: "sport filter parameter in queries"
      pattern: "eq.*sport"
    - from: "src/components/predictions/PredictionList.tsx"
      to: "src/lib/predictions.ts"
      via: "getLeagues accepts sport param"
      pattern: "getLeagues"
---

<objective>
Make the predictions frontend sport-aware -- remove hardcoded 'football' filtering, add sport tabs to the listing page, update prediction cards for NBA display (conference badge, spread info), and update detail pages for NBA context.

Purpose: Users see NBA predictions alongside football on the same predictions page with sport filtering via tabs.
Output: Updated PredictionList with sport tabs, sport-parameterized queries, NBA-ready card and detail components.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-nba-predictions-statistics/04-CONTEXT.md
@.planning/phases/04-nba-predictions-statistics/04-RESEARCH.md
@.planning/phases/03-football-predictions/03-02-SUMMARY.md
@.planning/phases/03-football-predictions/03-03-SUMMARY.md

@src/lib/predictions.ts
@src/components/predictions/PredictionList.tsx
@src/components/predictions/PredictionCard.astro
@src/components/predictions/PredictionDetail.tsx
@src/components/predictions/LeagueFilter.tsx
@src/i18n/ui.ts
@src/pages/en/predictions/[slug].astro
@src/pages/tl/hula/[slug].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sport-parameterize queries and add sport tabs to PredictionList</name>
  <files>
    src/lib/predictions.ts
    src/components/predictions/PredictionList.tsx
    src/i18n/ui.ts
  </files>
  <action>
    1. Update `src/lib/predictions.ts`:
       - `getTodayPredictions(sport?: string)`: remove hardcoded `.eq('sport', 'football')`. If sport param provided and not 'all', add `.eq('sport', sport)`. If no sport or 'all', return all sports mixed sorted by match_date ASC.
       - `getPastPredictions(page, perPage, sport?: string)`: same pattern -- parameterize sport filter, remove hardcoded 'football'.
       - `getLeagues(sport?: string)`: parameterize -- if sport provided, filter by it; otherwise return all active leagues.
       - Add `sport` to PREDICTION_FIELDS: add `sport, spread_line, total_line` to the select string.

    2. Update `src/components/predictions/PredictionList.tsx`:
       - Add sport state: `const [sport, setSport] = useState<string>('all');`
       - Add sport tabs above the Today/Past tabs: "All Sports" | "Football" | "Basketball" -- styled as pills/tabs matching site theme. Use hardcoded bilingual labels: { tl: { allSports: 'Lahat ng Isports', football: 'Football', basketball: 'Basketball' }, en: { allSports: 'All Sports', football: 'Football', basketball: 'Basketball' } }
       - Update fetchPredictions: replace hardcoded `.eq('sport', 'football')` with conditional: if sport !== 'all', add `.eq('sport', sport)`. Add `sport` dependency to useCallback deps.
       - Update fetchLeagues: if sport !== 'all', filter `.eq('sport', sport)` instead of hardcoded 'football'. Reset selectedLeague when sport changes.
       - Add sport to Prediction interface: `sport: string; spread_line?: number; total_line?: number;`
       - Update select fields string to include `sport, spread_line, total_line`
       - On NBA cards: show conference badge (if available via a future league/team join -- for now show sport icon: basketball emoji for basketball, football emoji for football) and pick type display. For NBA pick types (moneyline_home etc.), map to display labels: moneyline_home/moneyline_away -> 'ML', spread_home/spread_away -> 'SPREAD', over/under -> 'O/U'.
       - Add NBA pick type entries to pickTypeMap: { moneyline_home: 'ML', moneyline_away: 'ML', spread_home: 'SPREAD', spread_away: 'SPREAD', over: 'O/U', under: 'O/U' }
       - For spread picks, show the line value next to the label (e.g., "Lakers -5.5" with spread_line from prediction)

    3. Update `src/i18n/ui.ts`:
       - Add keys for statistics page (needed by Plan 03): predictions.allSports, predictions.football, predictions.basketball, stats.title, stats.winRate, stats.roi, stats.streak, stats.recentPicks, stats.profitChart, stats.noData, stats.allTime, stats.last7, stats.last30, stats.match, stats.pick, stats.result, stats.profit, stats.units
       - Both tl and en translations
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | tail -20</automated>
    <manual>Verify PredictionList renders sport tabs, queries are sport-parameterized, NBA pick types display correctly</manual>
  </verify>
  <done>PredictionList shows sport tabs (All/Football/Basketball), queries are parameterized, NBA picks display with ML/SPREAD/O/U labels, spread line shown on spread picks.</done>
</task>

<task type="auto">
  <name>Task 2: Update prediction cards and detail pages for NBA display</name>
  <files>
    src/components/predictions/PredictionCard.astro
    src/components/predictions/PredictionDetail.tsx
    src/pages/en/predictions/[slug].astro
    src/pages/tl/hula/[slug].astro
  </files>
  <action>
    1. Update `src/components/predictions/PredictionCard.astro`:
       - Add sport, spread_line, total_line to Props interface
       - Show sport indicator: small pill/badge showing sport type (Football/Basketball) with appropriate icon/color
       - For NBA spread picks: show spread line value (e.g., "-5.5") next to pick label
       - For NBA over/under picks: show total line value (e.g., "O/U 220.5")
       - Add NBA pick types to pickTypeMap (same as PredictionList: ML, SPREAD, O/U)

    2. Update `src/components/predictions/PredictionDetail.tsx`:
       - Add spread_line, total_line, sport to the fetched prediction fields
       - For NBA predictions: display spread line and total line in the prediction info section
       - Show sport badge in the match header area
       - For NBA spread picks: render "Spread: Lakers -5.5" in the prediction box
       - For NBA over/under picks: render "Total: Over 220.5" in the prediction box
       - Add NBA pick type labels to the detail view translations
       - Add hardcoded bilingual labels for NBA-specific terms: { tl: { spread: 'Spread', totalPoints: 'Kabuuang Puntos', moneyline: 'Moneyline', conference: 'Kumperensya' }, en: { spread: 'Spread', totalPoints: 'Total Points', moneyline: 'Moneyline', conference: 'Conference' } }

    3. Update `src/pages/en/predictions/[slug].astro` and `src/pages/tl/hula/[slug].astro`:
       - In getStaticPaths: remove the `.eq('sport', 'football')` filter if present, so both football and basketball predictions generate detail pages
       - Ensure the SportsEvent JSON-LD schema handles basketball sport type (update the schema builder if it references 'football' specifically)
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | tail -20</automated>
    <manual>Verify PredictionCard shows sport badge and NBA-specific info, detail pages handle both sports</manual>
  </verify>
  <done>PredictionCard.astro and PredictionDetail.tsx display NBA-specific information (spread, total line, sport badge). Detail pages generate paths for both football and basketball predictions.</done>
</task>

</tasks>

<verification>
- No hardcoded `.eq('sport', 'football')` remains in frontend query code (except LeagueFilter which correctly filters leagues by sport)
- PredictionList sport tabs switch between All/Football/Basketball
- NBA pick types (moneyline_home, spread_home, over, etc.) display with readable labels
- Spread line value appears on spread-type picks
- Detail pages render for both football and basketball slugs
- Astro check passes without type errors
</verification>

<success_criteria>
- Homepage "Today's Predictions" shows both sports mixed by time per user decision
- Shared predictions listing page with sport tabs per user decision (not separate pages)
- Same URL pattern for detail pages per user decision
- NBA prediction cards display conference/spread/total info per user decision
- All existing football functionality unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-nba-predictions-statistics/04-02-SUMMARY.md`
</output>
