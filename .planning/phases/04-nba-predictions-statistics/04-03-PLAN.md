---
phase: 04-nba-predictions-statistics
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - src/lib/statistics.ts
  - src/components/statistics/StatsDashboard.tsx
  - src/components/statistics/ProfitChart.tsx
  - src/components/statistics/StatCard.tsx
  - src/components/statistics/RecentPicks.tsx
  - src/pages/en/statistics/index.astro
  - src/pages/tl/estadistika/index.astro
  - src/i18n/routes.ts
  - package.json
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-04, STAT-05]

must_haves:
  truths:
    - "Statistics page shows overall win rate percentage with W/L count"
    - "Statistics page shows ROI percentage based on tracked odds with units display"
    - "Statistics page shows current streak (consecutive wins or losses)"
    - "User can filter statistics by sport (All Sports, Football, Basketball)"
    - "Profit chart shows cumulative profit over time as an interactive line chart"
    - "Time period filtering works (7 days, 30 days, All Time)"
    - "Recent picks table shows last 10 resolved picks with match, pick, result, profit"
  artifacts:
    - path: "src/lib/statistics.ts"
      provides: "Statistics computation from predictions table"
      exports: ["getStatistics"]
    - path: "src/components/statistics/StatsDashboard.tsx"
      provides: "Main React island for statistics page"
      contains: "StatsDashboard"
    - path: "src/components/statistics/ProfitChart.tsx"
      provides: "Recharts line chart for cumulative profit"
      contains: "LineChart"
    - path: "src/pages/en/statistics/index.astro"
      provides: "English statistics page"
      contains: "StatsDashboard"
    - path: "src/pages/tl/estadistika/index.astro"
      provides: "Filipino statistics page"
      contains: "StatsDashboard"
  key_links:
    - from: "src/components/statistics/StatsDashboard.tsx"
      to: "Supabase predictions table"
      via: "client-side queries with sport and period filters"
      pattern: "from.*predictions.*select"
    - from: "src/components/statistics/ProfitChart.tsx"
      to: "src/components/statistics/StatsDashboard.tsx"
      via: "receives profitHistory data as props"
      pattern: "ProfitChart"
    - from: "src/pages/en/statistics/index.astro"
      to: "src/components/statistics/StatsDashboard.tsx"
      via: "React island with client:load"
      pattern: "client:load"
---

<objective>
Build the statistics dashboard with win rate, ROI, streak hero cards, interactive Recharts profit chart, recent picks table, sport tabs, and time period filtering. Install Recharts and create bilingual statistics pages.

Purpose: Users can track prediction performance across all sports with visual charts and key metrics.
Output: Complete statistics page with hero stats, profit chart, recent picks, sport/period filtering.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-nba-predictions-statistics/04-CONTEXT.md
@.planning/phases/04-nba-predictions-statistics/04-RESEARCH.md
@.planning/phases/04-nba-predictions-statistics/04-02-SUMMARY.md

@src/i18n/ui.ts
@src/i18n/routes.ts
@src/pages/en/predictions/index.astro
@src/pages/tl/hula/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts, create statistics computation library and React components</name>
  <files>
    package.json
    src/lib/statistics.ts
    src/components/statistics/StatCard.tsx
    src/components/statistics/RecentPicks.tsx
    src/components/statistics/ProfitChart.tsx
    src/components/statistics/StatsDashboard.tsx
  </files>
  <action>
    1. Install Recharts: `cd /Users/mjcmatos/Desktop/mytaya && npm install recharts`

    2. Create `src/lib/statistics.ts`:
       - StatsResult interface: { totalPicks, wins, losses, pushes, winRate (number), roi (number), currentStreak: { type: 'win'|'loss', count: number }, profitHistory: { date: string, cumProfit: number, pickCount: number }[], recentPicks: { slug, pick, pick_label_en, pick_label_tl, result, odds, match_date, sport }[] }
       - `getStatistics(supabase, sport?: string, days?: number): Promise<StatsResult>` -- follows the pattern from 04-RESEARCH.md exactly:
         * Query predictions table: status='settled', published_site=true, ordered by settled_at ASC
         * If sport and sport !== 'all': filter by sport
         * If days: filter settled_at >= (now - days)
         * Compute wins, losses, pushes, winRate = wins/(wins+losses)*100
         * ROI: flat 1 unit per bet. Win profit = odds-1, loss = -1, push = 0. ROI = (totalProfit/totalPicks)*100
         * Streak: reverse data, count consecutive same result from most recent
         * ProfitHistory: cumulative profit per settled prediction (for chart)
         * RecentPicks: separate query, last 10 settled predictions ordered by settled_at DESC
       - `defaultStats(): StatsResult` -- returns zeros/empty arrays for empty state

    3. Create `src/components/statistics/StatCard.tsx`:
       - Props: { title: string, value: string, subtitle?: string, valueColor?: string }
       - Card with theme-aware background (var(--t-bg-card)), Bebas Neue display font for value
       - Responsive: stacks in a 3-column grid on desktop, full width on mobile

    4. Create `src/components/statistics/RecentPicks.tsx`:
       - Props: { picks: RecentPick[], lang: 'tl' | 'en' }
       - Table with columns: Match (pick label), Pick Type, Result (WIN/LOSS/PUSH badge), Profit (+X.XX / -1.00)
       - Hardcoded bilingual translations for column headers
       - Profit column: green for positive, red for negative
       - Link each row to prediction detail page

    5. Create `src/components/statistics/ProfitChart.tsx`:
       - Props: { data: { date: string, cumProfit: number, pickCount: number }[] }
       - Use Recharts: ResponsiveContainer (height=300), LineChart, Line (stroke=brand-primary), XAxis (date formatted MMM d), YAxis (units with +/- prefix), Tooltip (theme-aware styling using CSS vars), CartesianGrid (dashed)
       - Follow the exact pattern from 04-RESEARCH.md code example
       - Line: monotone interpolation, no dots, active dot on hover
       - Empty state: show message when data array is empty

    6. Create `src/components/statistics/StatsDashboard.tsx`:
       - Props: { lang: 'tl' | 'en' }
       - State: sport ('all'), period (null for all time, 7, 30), stats (StatsResult), loading
       - Create inline Supabase client (same pattern as PredictionList -- createClient with PUBLIC env vars)
       - Hardcoded bilingual translations for all labels (same pattern as other React islands):
         * allSports, football, basketball, winRate, roi, streak, recentPicks, profitChart, noData, allTime, last7d, last30d, units, winsLabel, lossesLabel
       - Layout (top to bottom per user decision):
         a. Sport filter tabs: All Sports | Football | Basketball (pill-style, brand-primary active)
         b. Time period chips: 7 days | 30 days | All Time (smaller chips below tabs)
         c. Hero stat cards grid (3 columns): Win Rate card (value: "65.2%", subtitle: "130W - 70L"), ROI card (value: "+12.5%", subtitle: "+25.0 units"), Streak card (value: "W5" or "L3", color green for win red for loss)
         d. Profit chart (ProfitChart component)
         e. Recent picks table (RecentPicks component, last 10)
       - useEffect: fetch stats when sport or period changes. Call getStatistics with supabase client, sport, period.
       - Loading: show skeleton placeholders for stat cards and chart
       - Empty state: when no data, show centered message with chart icon
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | tail -20</automated>
    <manual>Verify StatsDashboard renders all sections, ProfitChart uses Recharts correctly</manual>
  </verify>
  <done>Statistics library computes win rate, ROI, streak, and profit history from predictions. All React components render: StatCard, ProfitChart (Recharts), RecentPicks table, StatsDashboard orchestrator with sport/period filters.</done>
</task>

<task type="auto">
  <name>Task 2: Create bilingual statistics pages and update i18n routes</name>
  <files>
    src/pages/en/statistics/index.astro
    src/pages/tl/estadistika/index.astro
    src/i18n/routes.ts
  </files>
  <action>
    1. Create `src/pages/en/statistics/index.astro`:
       - Import Layout from '@/layouts/Layout.astro'
       - Import StatsDashboard from '@/components/statistics/StatsDashboard'
       - Set title, description meta (useTranslations pattern from existing pages)
       - Render StatsDashboard with `client:load` and `lang="en"`
       - Page title: "Prediction Statistics" / section heading
       - Add custom hreflang tags in head slot for /en/statistics/ <-> /tl/estadistika/ mapping (same pattern as prediction detail pages)

    2. Create `src/pages/tl/estadistika/index.astro`:
       - Same structure as English version
       - lang="tl", Filipino title: "Estadistika ng Hula"
       - Correct hreflang cross-linking

    3. Update `src/i18n/routes.ts`:
       - Add route mappings: tl: '/estadistika/' -> '/statistics/', en: '/statistics/' -> '/estadistika/'

    Note: i18n keys for stat labels were already added in Plan 02 Task 1. If they weren't added there (execution order), add them here in src/i18n/ui.ts as fallback.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | tail -20 && grep -c "estadistika\|statistics" src/i18n/routes.ts</automated>
    <manual>Verify both /en/statistics/ and /tl/estadistika/ pages exist and render StatsDashboard island</manual>
  </verify>
  <done>Bilingual statistics pages exist at /en/statistics/ and /tl/estadistika/ with correct hreflang cross-linking. StatsDashboard renders as client:load React island. Route mapping added to i18n.</done>
</task>

</tasks>

<verification>
- `npm ls recharts` shows recharts installed
- Statistics pages load at /en/statistics/ and /tl/estadistika/
- StatsDashboard shows hero stat cards, profit chart, and recent picks table
- Sport tabs filter statistics by sport
- Time period chips filter by 7d/30d/all time
- Profit chart renders as interactive SVG line chart with tooltips
- Empty state displays when no settled predictions exist
- Astro check passes without errors
</verification>

<success_criteria>
- Hero stat cards show Win Rate, ROI, Current Streak per user decision
- Sport filter tabs: All Sports | Football | Basketball per user decision
- Time period chips: 7 days | 30 days | All Time per user decision
- Line chart shows cumulative profit in units per user decision
- Interactive tooltips on chart per user decision
- Recent picks table shows last 10 resolved picks per user decision
- Chart syncs with sport and time period filters per user decision
- Recharts used for charting per user decision (lightweight JS library with React islands)
</success_criteria>

<output>
After completion, create `.planning/phases/04-nba-predictions-statistics/04-03-SUMMARY.md`
</output>
