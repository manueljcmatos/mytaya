---
phase: 04-nba-predictions-statistics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/004_nba_support.sql
  - workers/predictions-worker/src/types.ts
  - workers/predictions-worker/src/api-basketball.ts
  - workers/predictions-worker/src/prediction-gen.ts
  - workers/predictions-worker/src/resolver.ts
  - workers/predictions-worker/src/index.ts
  - workers/predictions-worker/wrangler.toml
autonomous: true
requirements: [BASK-01, BASK-03]

must_haves:
  truths:
    - "NBA games are fetched daily from API-Basketball with correct league ID and season"
    - "NBA predictions are generated with spread, moneyline, and over/under pick types"
    - "NBA games are resolved automatically using basketball-specific logic (no draws)"
    - "Spread and total line values are stored for correct resolution of NBA bets"
  artifacts:
    - path: "supabase/migrations/004_nba_support.sql"
      provides: "spread_line, total_line columns on predictions + conference on teams + NBA league seed + stats index"
      contains: "spread_line"
    - path: "workers/predictions-worker/src/api-basketball.ts"
      provides: "API-Basketball client for NBA games and odds"
      exports: ["fetchNbaGames", "fetchNbaOdds", "NBA_LEAGUE"]
    - path: "workers/predictions-worker/src/types.ts"
      provides: "NBA-specific types and extended pick types"
      contains: "NbaPickType"
    - path: "workers/predictions-worker/src/resolver.ts"
      provides: "NBA result determination with spread/total logic"
      contains: "determineNbaResult"
  key_links:
    - from: "workers/predictions-worker/src/index.ts"
      to: "workers/predictions-worker/src/api-basketball.ts"
      via: "import fetchNbaGames in scheduled handler"
      pattern: "import.*api-basketball"
    - from: "workers/predictions-worker/src/resolver.ts"
      to: "supabase predictions table"
      via: "queries pending basketball predictions and resolves with NBA logic"
      pattern: "sport.*basketball"
---

<objective>
Extend the predictions worker to support NBA basketball -- API-Basketball data fetching, NBA-specific prediction generation, basketball resolution logic, and database schema migration for spread/total lines.

Purpose: Enable automated NBA prediction pipeline alongside the existing football pipeline, using the same Worker infrastructure.
Output: Extended predictions-worker with NBA module, NBA resolver, updated types, and migration SQL.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-nba-predictions-statistics/04-CONTEXT.md
@.planning/phases/04-nba-predictions-statistics/04-RESEARCH.md
@.planning/phases/03-football-predictions/03-01-SUMMARY.md

@workers/predictions-worker/src/index.ts
@workers/predictions-worker/src/types.ts
@workers/predictions-worker/src/api-football.ts
@workers/predictions-worker/src/prediction-gen.ts
@workers/predictions-worker/src/resolver.ts
@workers/predictions-worker/src/supabase.ts
@workers/predictions-worker/wrangler.toml
@supabase/migrations/00000000000000_foundation.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration and API-Basketball client module</name>
  <files>
    supabase/migrations/004_nba_support.sql
    workers/predictions-worker/src/api-basketball.ts
    workers/predictions-worker/src/types.ts
  </files>
  <action>
    1. Create `supabase/migrations/004_nba_support.sql`:
       - ALTER predictions table: ADD COLUMN spread_line DECIMAL(5,1), ADD COLUMN total_line DECIMAL(5,1)
       - ALTER teams table: ADD COLUMN conference TEXT CHECK (conference IN ('Eastern', 'Western'))
       - INSERT NBA league into leagues table: name='NBA', slug='nba', sport='basketball', country='USA', api_source='api-sports', api_league_id=12, is_active=true (ON CONFLICT slug DO NOTHING)
       - CREATE INDEX idx_predictions_stats ON predictions (sport, status, settled_at) WHERE status='settled' AND published_site=true

    2. Create `workers/predictions-worker/src/api-basketball.ts`:
       - Base URL: https://v1.basketball.api-sports.io
       - NBA_LEAGUE constant: { id: 12, name: 'NBA', slug: 'nba', season: '2025-2026' }
       - `fetchNbaGames(apiKey: string, date: string): Promise<ApiBasketballGame[]>` -- GET /games?league=12&season=2025-2026&date={date}, same x-apisports-key header as football
       - `fetchNbaOdds(apiKey: string, gameId: number): Promise<NbaOdds | null>` -- GET /odds?game={gameId}, parse spread (handicap), moneyline (h2h), and totals from first bookmaker
       - ApiBasketballGame interface: { id, date, timestamp, status: { short, long }, league: { id, name, season }, teams: { home: { id, name, logo }, away: { id, name, logo } }, scores: { home: { total }, away: { total } } }
       - NbaOdds interface: { moneyline: { home, away }, spread: { home_line, home_odds, away_odds }, totals: { line, over_odds, under_odds }, source: string }
       - Handle empty/error responses gracefully (return [] or null)

    3. Update `workers/predictions-worker/src/types.ts`:
       - Add NbaPickType union: 'moneyline_home' | 'moneyline_away' | 'spread_home' | 'spread_away' | 'over' | 'under'
       - Add NbaMatchContext interface extending MatchContext with spread and totalLine fields
       - Extend PredictionInsert: make sport 'football' | 'basketball', add optional spread_line and total_line number fields
       - Keep existing PickType unchanged (football uses it)
       - Add AllPickType = PickType | NbaPickType union for the combined type
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && cat supabase/migrations/004_nba_support.sql | grep -c "spread_line\|total_line\|conference\|NBA\|idx_predictions_stats" && cd workers/predictions-worker && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify migration SQL is valid, api-basketball.ts has correct endpoint URLs, types compile</manual>
  </verify>
  <done>Migration file adds spread_line, total_line, conference columns plus NBA league seed. API-Basketball client fetches games and odds. Types extended for NBA pick types and PredictionInsert supports basketball.</done>
</task>

<task type="auto">
  <name>Task 2: NBA prediction generation, resolution, and worker dispatch</name>
  <files>
    workers/predictions-worker/src/prediction-gen.ts
    workers/predictions-worker/src/resolver.ts
    workers/predictions-worker/src/index.ts
    workers/predictions-worker/wrangler.toml
  </files>
  <action>
    1. Update `workers/predictions-worker/src/prediction-gen.ts`:
       - Add `generateNbaPrediction(ai: Ai, match: NbaMatchContext): Promise<NbaPredictionResult>` function
       - NBA-specific prompt: mention spread, moneyline, over/under as pick types. Include spread line and total line in context. Pick types are 'moneyline_home', 'moneyline_away', 'spread_home', 'spread_away', 'over', 'under'
       - Same Workers AI model (@cf/meta/llama-3.1-8b-instruct), same JSON parsing with regex, same validation
       - Fallback: if AI fails, use moneyline pick for the team with lower moneyline odds
       - Keep existing generatePrediction for football unchanged

    2. Update `workers/predictions-worker/src/resolver.ts`:
       - Add `determineNbaResult(pick: string, homeScore: number, awayScore: number, spreadLine?: number, totalLine?: number): 'win' | 'loss' | 'push'` function per research pattern. Half-point lines (e.g., -5.5) prevent pushes on spread/totals. Moneyline: no draws in NBA.
       - Add `resolveNbaMatches(env: Env): Promise<void>` function: query pending basketball predictions with api_fixture_id, fetch game result from API-Basketball (status.short === 'FT'), determine result using determineNbaResult with spread_line and total_line from prediction row, update status to settled
       - Import fetchNbaGameResult from api-basketball.ts (add this function: GET /games?id={gameId} returning single game)
       - Keep existing football resolver unchanged

    3. Update `workers/predictions-worker/src/index.ts`:
       - Add `fetchAndGenerateNbaPredictions(env: Env)` function mirroring the football one:
         * Fetch today's NBA games via fetchNbaGames
         * If no games, log "No NBA games today" and return gracefully (offseason handling)
         * Select 3-5 games via existing selectBestFixtures (works for any fixture array)
         * For each game: fetch odds, upsert teams (store conference on team if available), upsert NBA league, generate prediction, build slug (nba-home-vs-away-date), insert prediction with sport='basketball', spread_line, and total_line
       - Update scheduled handler: '0 6 * * *' cron now calls both fetchAndGeneratePredictions AND fetchAndGenerateNbaPredictions
       - Update '*/30 * * * *' cron to call both resolveFinishedMatches AND resolveNbaMatches
       - Wrap NBA calls in try/catch so football pipeline continues even if NBA fails

    4. Update `workers/predictions-worker/wrangler.toml`:
       - Add comment for API key note: same x-apisports-key works for both API-Football and API-Basketball (no new secret needed)
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya/workers/predictions-worker && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify NBA pipeline dispatches from cron, resolver handles NBA-specific logic, offseason produces no errors</manual>
  </verify>
  <done>Worker generates NBA predictions daily alongside football, resolves NBA games with spread/total logic, handles offseason gracefully, and football pipeline remains unaffected.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors in workers/predictions-worker
- Migration SQL contains all required schema changes (spread_line, total_line, conference, NBA seed, stats index)
- Worker index.ts dispatches both football and NBA on both cron triggers
- NBA resolver uses determineNbaResult (not football's determineResult) for basketball predictions
- Existing football pipeline code paths remain unchanged
</verification>

<success_criteria>
- NBA prediction pipeline extends existing worker without breaking football
- API-Basketball client follows same patterns as api-football.ts (rate limiting, error handling)
- NBA pick types (moneyline, spread, over/under) are properly typed and stored
- Spread and total line values are persisted for resolution
- Offseason (no NBA games) handled gracefully with log message, no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-nba-predictions-statistics/04-01-SUMMARY.md`
</output>
