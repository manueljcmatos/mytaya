---
phase: 05-blog-automation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/posts.ts
  - src/components/blog/BlogList.tsx
  - src/pages/en/blog/index.astro
  - src/pages/tl/blog/index.astro
  - src/pages/en/blog/[slug].astro
  - src/pages/tl/blog/[slug].astro
  - src/pages/news-sitemap.xml.ts
  - src/i18n/ui.ts
  - src/i18n/routes.ts
autonomous: true
requirements: [BLOG-03, BLOG-04, BLOG-05]

must_haves:
  truths:
    - "Blog index page shows a featured hero article at top and a grid of recent articles below"
    - "User can filter blog articles by sport (All, Football, Basketball) using tabs"
    - "Blog index supports pagination with 12 articles per page"
    - "Individual blog post pages display full bilingual article content with proper SEO metadata"
    - "Blog posts and prediction detail pages cross-link to each other"
    - "News sitemap at /news-sitemap.xml includes blog posts from the last 2 days"
  artifacts:
    - path: "src/lib/posts.ts"
      provides: "Supabase query helpers for blog posts"
      exports: ["getBlogPosts", "getBlogPostBySlug", "getAllBlogSlugs"]
    - path: "src/components/blog/BlogList.tsx"
      provides: "React island with sport tabs, pagination, and article cards"
      min_lines: 100
    - path: "src/pages/en/blog/[slug].astro"
      provides: "English blog post detail page with BlogPosting schema"
      contains: "buildBlogPostingSchema"
    - path: "src/pages/tl/blog/[slug].astro"
      provides: "Filipino blog post detail page with BlogPosting schema"
      contains: "buildBlogPostingSchema"
    - path: "src/pages/news-sitemap.xml.ts"
      provides: "Populated news sitemap with recent blog posts"
      contains: "news:news"
  key_links:
    - from: "src/components/blog/BlogList.tsx"
      to: "supabase posts table"
      via: "inline Supabase client fetch with pagination"
      pattern: "from\\('posts'\\)"
    - from: "src/pages/en/blog/[slug].astro"
      to: "src/lib/seo.ts"
      via: "buildBlogPostingSchema for JSON-LD"
      pattern: "buildBlogPostingSchema"
    - from: "src/pages/en/blog/[slug].astro"
      to: "src/lib/posts.ts"
      via: "getStaticPaths query"
      pattern: "getAllBlogSlugs"
    - from: "src/pages/news-sitemap.xml.ts"
      to: "supabase posts table"
      via: "Supabase query with 2-day filter"
      pattern: "gte.*published_at"
---

<objective>
Build the blog frontend: index pages with featured article + grid + sport tabs + pagination, individual post detail pages with SEO schema, and populate the news sitemap.

Purpose: Make AI-generated blog content accessible to users and search engines, driving organic traffic through long-tail match preview keywords.
Output: BlogList React island, blog index pages (en/tl), blog detail pages (en/tl), query helpers, populated news sitemap
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-blog-automation/05-CONTEXT.md
@.planning/phases/05-blog-automation/05-RESEARCH.md
@.planning/phases/05-blog-automation/05-01-SUMMARY.md

@src/pages/en/blog/index.astro
@src/pages/tl/blog/index.astro
@src/pages/en/predictions/[slug].astro
@src/pages/tl/hula/[slug].astro
@src/components/PredictionList.tsx
@src/lib/predictions.ts
@src/lib/seo.ts
@src/lib/supabase.ts
@src/i18n/ui.ts
@src/i18n/routes.ts
@src/pages/news-sitemap.xml.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query helpers, BlogList island, and blog index pages</name>
  <files>
    src/lib/posts.ts
    src/components/blog/BlogList.tsx
    src/pages/en/blog/index.astro
    src/pages/tl/blog/index.astro
    src/i18n/ui.ts
  </files>
  <action>
    1. Create `src/lib/posts.ts` -- Supabase query helpers following `src/lib/predictions.ts` pattern:
       - `getBlogPosts(page, perPage, sport?)`: paginated posts query with optional sport filter, returns `{ data, count }`. Filter `is_published = true`, order by `published_at DESC`, use `.range(from, to)` for pagination. If sport param is provided and not 'all', add `.eq('sport', sport)`.
       - `getBlogPostBySlug(slug, lang)`: fetch single post by slug_en or slug_tl depending on lang param. Join predictions table to get linked prediction slug for cross-linking.
       - `getAllBlogSlugs()`: fetch all published post slug_en and slug_tl values for getStaticPaths.
       - Use conditional supabase import pattern (null-safe, return empty arrays if no client).

    2. Create `src/components/blog/BlogList.tsx` -- React island following PredictionList.tsx pattern:
       - Inline Supabase client creation (same pattern as PredictionList)
       - Hardcoded bilingual translations object keyed by lang prop
       - Sport tabs: "All" | "Football" | "Basketball" -- pill buttons consistent with PredictionList sport tabs
       - Featured article section: first article rendered as hero card with larger title, full excerpt, sport badge, date, read time
       - Grid section: remaining articles in responsive grid (1 col mobile, 2 cols md, 3 cols lg)
       - Article card shows: title (title_en or title_tl based on lang), 1-2 line excerpt, sport badge (teal for football, orange for basketball), date (Intl.DateTimeFormat Asia/Manila), estimated read time in minutes
       - No images on cards per user decision
       - Pagination: page number buttons at bottom, 12 articles per page
       - Each card links to `/{lang}/blog/{slug_en or slug_tl}/`
       - Empty state: use text "No articles yet." / "Wala pang mga artikulo." when no posts exist
       - Style with Tailwind classes using CSS custom properties (--t-* tokens) for theme awareness
       - Result badge on cards: if the linked prediction has a result, show a small WIN/LOSS/PUSH badge on the article card

    3. Update `src/pages/en/blog/index.astro` and `src/pages/tl/blog/index.astro`:
       - Replace EmptyState placeholder with BlogList React island (`client:load`)
       - Pass `lang` prop
       - Keep Layout wrapper with proper title
       - Add page heading "Blog" / "Blog" (same in both languages per existing nav)

    4. Add i18n keys to `src/i18n/ui.ts` for blog-specific strings if needed:
       - `'blog.featured'`: 'Tampok na Artikulo' / 'Featured Article'
       - `'blog.readMore'`: 'Basahin pa' / 'Read more'
       - `'blog.readTime'`: '{min} min basahin' / '{min} min read'
       - `'blog.filterAll'`: 'Lahat' / 'All'
       - Note: Since BlogList is a React island, these will be hardcoded in the component (consistent with PredictionList pattern), but add the keys to ui.ts for potential SSG usage.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | head -30</automated>
    <manual>Check blog index renders BlogList island with sport tabs and pagination</manual>
  </verify>
  <done>
    - posts.ts exports getBlogPosts, getBlogPostBySlug, getAllBlogSlugs
    - BlogList.tsx renders featured hero + article grid with sport tabs and pagination
    - Article cards show title, excerpt, sport badge, date, read time (no images)
    - Blog index pages load BlogList island instead of EmptyState
    - 12 articles per page with pagination controls
  </done>
</task>

<task type="auto">
  <name>Task 2: Blog detail pages, cross-linking, and news sitemap</name>
  <files>
    src/pages/en/blog/[slug].astro
    src/pages/tl/blog/[slug].astro
    src/pages/news-sitemap.xml.ts
    src/i18n/routes.ts
  </files>
  <action>
    1. Create `src/pages/en/blog/[slug].astro` -- following `src/pages/en/predictions/[slug].astro` pattern:
       - `getStaticPaths()`: fetch all published posts via getAllBlogSlugs(), map slug_en values to params
       - Frontmatter: fetch full post by slug_en using getBlogPostBySlug, get linked prediction slug for cross-link
       - Build BlogPosting JSON-LD using `buildBlogPostingSchema()` from `src/lib/seo.ts`:
         ```typescript
         buildBlogPostingSchema({
           title: post.title_en,
           description: post.excerpt_en,
           datePublished: post.published_at,
           url: `${SITE.url}/en/blog/${post.slug_en}/`,
           lang: 'en',
         })
         ```
       - Render article content as HTML (content is stored as markdown-like text, render with basic paragraph/heading formatting)
       - Include breadcrumb: Home > Blog > Article Title
       - Cross-link to prediction detail page: "View our prediction" link to `/en/predictions/{prediction_slug}/`
       - Hreflang tags: link to Filipino version at `/tl/blog/{slug_tl}/`
       - Result section rendering: if content contains "## Result", render that section with a visually distinct callout box:
         - WIN: green background (`bg-emerald-500/10 border-emerald-500`)
         - LOSS: red background (`bg-red-500/10 border-red-500`)
         - PUSH: amber background (`bg-amber-500/10 border-amber-500`)
         - Detect result keyword in the "## Result" / "## Resulta" section

    2. Create `src/pages/tl/blog/[slug].astro` -- mirror of English version:
       - Uses slug_tl for params and routing
       - Displays title_tl and content_tl
       - Cross-link to `/tl/hula/{prediction_slug}/` (Filipino prediction path)
       - Hreflang tags link to English version at `/en/blog/{slug_en}/`

    3. Update `src/i18n/routes.ts` -- add blog detail route mapping:
       - The blog index route `/blog/` already exists in both locales
       - No additional route mapping needed since blog detail pages use `[slug]` dynamic routes with locale-specific slugs (slug_en vs slug_tl)

    4. Update `src/pages/news-sitemap.xml.ts` -- populate with real blog posts:
       - Import supabase client from `src/lib/supabase.ts`
       - Query posts table: `is_published = true`, `published_at >= NOW() - 2 days` (Google News requirement per research pitfall #3)
       - Generate 2 entries per post (EN + TL):
         ```xml
         <url>
           <loc>https://mytaya.com/en/blog/{slug_en}/</loc>
           <news:news>
             <news:publication>
               <news:name>MyTaya</news:name>
               <news:language>en</news:language>
             </news:publication>
             <news:publication_date>{published_at}</news:publication_date>
             <news:title>{title_en}</news:title>
           </news:news>
         </url>
         ```
       - Handle case where supabase client is null (return empty sitemap)
       - Use SITE.url from constants for absolute URLs
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | head -30</automated>
    <manual>Verify blog detail pages render with BlogPosting schema and cross-links to predictions</manual>
  </verify>
  <done>
    - Blog detail pages exist at /en/blog/[slug] and /tl/blog/[slug] with bilingual content
    - BlogPosting JSON-LD schema included via buildBlogPostingSchema
    - Hreflang cross-linking between EN and TL blog posts
    - Cross-links between blog articles and prediction detail pages (bidirectional)
    - Result sections render with colored callout boxes (green/red/amber)
    - News sitemap populated with posts from last 2 days in both languages
    - Empty/null supabase client handled gracefully
  </done>
</task>

</tasks>

<verification>
- `npx astro check` passes
- Blog index pages load BlogList island with sport tabs
- Blog detail pages use buildBlogPostingSchema for SEO
- News sitemap generates valid XML with recent blog posts
- Cross-linking: blog -> prediction and prediction detail should be able to link back to blog
</verification>

<success_criteria>
- Blog index shows featured article + grid with sport filtering and pagination (12/page)
- Individual blog pages display full bilingual content with BlogPosting schema
- Result appendix sections are visually distinct (colored callout)
- News sitemap includes posts from last 2 days with bilingual entries
- All pages follow established patterns (Layout, i18n, Supabase client, theme tokens)
</success_criteria>

<output>
After completion, create `.planning/phases/05-blog-automation/05-02-SUMMARY.md`
</output>
