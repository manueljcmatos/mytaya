---
phase: 02-seo-compliance-affiliates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo.ts
  - src/pages/news-sitemap.xml.ts
  - src/i18n/routes.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04

must_haves:
  truths:
    - "SportsEvent JSON-LD generator produces valid Schema.org markup with homeTeam, awayTeam, startDate, sport"
    - "BlogPosting JSON-LD generator produces valid Schema.org markup with headline, datePublished, author, inLanguage"
    - "Organization JSON-LD generator produces site-wide trust signal with name, url, logo"
    - "BreadcrumbList JSON-LD generator produces navigation structure markup"
    - "News sitemap endpoint returns valid XML at /news-sitemap.xml with Google News namespace"
  artifacts:
    - path: "src/lib/seo.ts"
      provides: "Schema.org JSON-LD builder functions"
      exports: ["buildSportsEventSchema", "buildBlogPostingSchema", "buildOrganizationSchema", "buildBreadcrumbSchema"]
    - path: "src/pages/news-sitemap.xml.ts"
      provides: "Google News sitemap endpoint"
      exports: ["GET"]
  key_links:
    - from: "src/lib/seo.ts"
      to: "src/layouts/Layout.astro"
      via: "schema prop accepts return value of builder functions"
      pattern: "schema.*@context.*schema\\.org"
---

<objective>
Create Schema.org JSON-LD builder functions and a Google News sitemap endpoint.

Purpose: Prediction and blog pages need structured data for Google Rich Results (YMYL trust). The news sitemap enables Google News eligibility for blog content. These utility functions are consumed by pages in this and future phases.
Output: src/lib/seo.ts with 4 schema builders, src/pages/news-sitemap.xml.ts endpoint
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-seo-compliance-affiliates/02-RESEARCH.md
@src/layouts/Layout.astro
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Schema.org JSON-LD builder functions</name>
  <files>src/lib/seo.ts</files>
  <action>
Create src/lib/seo.ts with four exported functions:

1. `buildSportsEventSchema(prediction)` - accepts {title, homeTeam, awayTeam, sport, startDate (ISO 8601), venue?, url, description?}. Returns JSON-LD object with @context, @type SportsEvent, homeTeam/awayTeam as SportsTeam, eventAttendanceMode, eventStatus. Include all required properties per Schema.org spec to pass Google Rich Results Test.

2. `buildBlogPostingSchema(post)` - accepts {title, description, datePublished, dateModified?, author?, image?, url, lang: 'tl'|'en'}. Returns JSON-LD with @type BlogPosting, publisher as Organization (MyTaya), inLanguage (fil for tl, en for en), mainEntityOfPage.

3. `buildOrganizationSchema()` - returns site-wide Organization JSON-LD with name "MyTaya", url "https://mytaya.com", logo pointing to /icon-512.png. This is a YMYL trust signal.

4. `buildBreadcrumbSchema(items)` - accepts array of {name, url} objects. Returns BreadcrumbList JSON-LD with proper itemListElement positions.

All functions return plain objects (not strings) -- Layout.astro already handles JSON.stringify via the schema prop. Use SITE constant from constants.ts for url/name.

Type all function parameters with TypeScript interfaces exported from the same file.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx tsc --noEmit src/lib/seo.ts 2>&1 | head -20</automated>
    <manual>Review that each function returns valid JSON-LD structure with @context and @type</manual>
  </verify>
  <done>Four exported functions (buildSportsEventSchema, buildBlogPostingSchema, buildOrganizationSchema, buildBreadcrumbSchema) exist in src/lib/seo.ts, type-check clean, and return properly structured JSON-LD objects compatible with Layout.astro's schema prop</done>
</task>

<task type="auto">
  <name>Task 2: Create Google News sitemap endpoint and update route map</name>
  <files>src/pages/news-sitemap.xml.ts, src/i18n/routes.ts</files>
  <action>
Create src/pages/news-sitemap.xml.ts as an Astro static endpoint (export const GET: APIRoute):

- Generate XML with xmlns:news namespace per Google News sitemap spec
- For now, return a valid but empty news sitemap (no blog posts exist yet -- Phase 5 will populate it)
- Include XML declaration, urlset with sitemaps.org and news namespaces
- Add a comment explaining this will be populated when blog posts are created in Phase 5
- Content-Type header must be application/xml

The endpoint must work in static mode (no SSR). Astro static endpoints with .xml.ts extension generate at build time.

Also update src/i18n/routes.ts to add the responsible gambling page routes that Plan 02 will create:
- tl: '/responsableng-pagsusugal/' maps to en: '/responsible-gambling/'
- en: '/responsible-gambling/' maps to tl: '/responsableng-pagsusugal/'

This prevents Plan 02 from having a file conflict with this plan on routes.ts.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro build 2>&1 | tail -20</automated>
    <manual>Check that build output includes news-sitemap.xml in the generated files</manual>
  </verify>
  <done>news-sitemap.xml.ts generates valid XML at build time. Route map includes responsible gambling page mappings for both languages.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors on seo.ts
- `npx astro build` completes successfully and generates news-sitemap.xml
- Each schema builder function returns an object with "@context": "https://schema.org"
</verification>

<success_criteria>
- src/lib/seo.ts exports 4 typed schema builder functions
- src/pages/news-sitemap.xml.ts generates valid Google News sitemap XML
- All functions are compatible with Layout.astro's schema prop (plain objects, not strings)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-seo-compliance-affiliates/02-01-SUMMARY.md`
</output>
