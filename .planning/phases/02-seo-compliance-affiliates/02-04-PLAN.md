---
phase: 02-seo-compliance-affiliates
plan: 04
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/components/LeadCaptureForm.tsx
  - src/lib/schemas.ts
  - supabase/migrations/002_leads_table.sql
  - src/pages/tl/index.astro
  - src/pages/en/index.astro
autonomous: true
requirements:
  - AFFL-04

must_haves:
  truths:
    - "Lead capture form validates email using Zod and shows validation errors"
    - "Form submits email to Supabase leads table via client-side anon key insert"
    - "Lead capture form appears on homepage in both languages"
    - "Form shows success state with Telegram channel CTA after submission"
    - "Supabase leads table has RLS enabled with INSERT-only policy for anon"
  artifacts:
    - path: "src/components/LeadCaptureForm.tsx"
      provides: "React island for email capture with Zod validation"
      contains: "z.string().email()"
    - path: "src/lib/schemas.ts"
      provides: "Zod validation schemas"
      exports: ["leadSchema"]
    - path: "supabase/migrations/002_leads_table.sql"
      provides: "Leads table DDL with RLS"
      contains: "ENABLE ROW LEVEL SECURITY"
  key_links:
    - from: "src/components/LeadCaptureForm.tsx"
      to: "@supabase/supabase-js"
      via: "client-side Supabase insert"
      pattern: "supabase.*from.*leads.*insert"
    - from: "src/components/LeadCaptureForm.tsx"
      to: "src/lib/schemas.ts"
      via: "imports leadSchema for validation"
      pattern: "import.*leadSchema"
---

<objective>
Build the lead capture form with Zod email validation and Supabase storage, placed on the homepage in both languages.

Purpose: Capture email leads for newsletter/tips distribution and funnel users to Telegram. Email-only form (lower friction = higher conversion). This is a React island for client-side interactivity since the site is static (no SSR).
Output: LeadCaptureForm React component, Zod schemas, Supabase migration, updated homepages
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-seo-compliance-affiliates/02-RESEARCH.md
@.planning/phases/02-seo-compliance-affiliates/02-02-SUMMARY.md
@src/lib/supabase.ts
@src/pages/tl/index.astro
@src/pages/en/index.astro
@src/i18n/ui.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Zod, create validation schemas and Supabase migration</name>
  <files>
    src/lib/schemas.ts
    supabase/migrations/002_leads_table.sql
  </files>
  <action>
Install Zod: `npm install zod`

Create src/lib/schemas.ts:
- Export `leadSchema` using Zod: z.object({ email: z.string().min(1).email(), lang: z.enum(['tl', 'en']).default('tl') })
- Export the inferred type: `export type LeadInput = z.infer<typeof leadSchema>`
- This file will hold all Zod schemas for the project (just leads for now)

Create supabase/migrations/002_leads_table.sql (create supabase/migrations/ directory if it doesn't exist):
```sql
CREATE TABLE IF NOT EXISTS leads (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  email TEXT NOT NULL,
  source TEXT DEFAULT 'website',
  lang TEXT DEFAULT 'tl',
  created_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE leads ADD CONSTRAINT leads_email_unique UNIQUE (email);
ALTER TABLE leads ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow anonymous insert" ON leads FOR INSERT TO anon WITH CHECK (true);
```
Add a comment at top: "-- Run this migration in Supabase SQL editor before the lead capture form goes live"

This is a migration file for documentation/reference. The user will run it manually in Supabase SQL editor.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx tsc --noEmit src/lib/schemas.ts 2>&1 | head -10</automated>
    <manual>Verify Zod is in package.json dependencies and schemas.ts exports leadSchema</manual>
  </verify>
  <done>Zod installed. leadSchema validates email with proper error messages. Migration SQL ready for Supabase with RLS enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Create LeadCaptureForm component and add to homepages</name>
  <files>
    src/components/LeadCaptureForm.tsx
    src/pages/tl/index.astro
    src/pages/en/index.astro
  </files>
  <action>
Create src/components/LeadCaptureForm.tsx (React island):
- Props: lang ('tl' | 'en')
- Import { leadSchema } from '../lib/schemas'
- Import { createClient } from '@supabase/supabase-js' -- create client inline using import.meta.env.PUBLIC_SUPABASE_URL and import.meta.env.PUBLIC_SUPABASE_ANON_KEY (client-side env vars must use PUBLIC_ prefix in Astro)
- State: email (string), status ('idle' | 'loading' | 'success' | 'error'), errorMsg (string)
- On submit: validate with leadSchema.safeParse, if invalid show error, if valid insert to Supabase leads table via client.from('leads').upsert({email, lang, source: 'website'}, {onConflict: 'email'})
- If Supabase env vars not set, show a graceful "Coming soon" state (not a crash)
- Success state: "Thanks for subscribing!" message + Telegram CTA button linking to https://t.me/mytaya
- Use translation strings from i18n (pass as props or hardcode bilingual -- since React component can't use Astro's useTranslations, hardcode a simple translations object inside the component using the lang prop)
- Translation keys to use: lead.emailPlaceholder, lead.subscribe, lead.cta, lead.success, lead.error, lead.telegramCta (these were added to ui.ts in Plan 02)
- Styling: use Tailwind classes. Email input + submit button inline on desktop, stacked on mobile. Brand primary color for button. Matches site design.
- Form section has a heading: "Get free daily predictions" / "Makakuha ng libreng hula araw-araw"

Update src/pages/tl/index.astro and src/pages/en/index.astro:
- Import LeadCaptureForm from components
- Add a lead capture section ABOVE the operators section (between TelegramBanner and Blog sections, or between Blog and Operators -- use judgment for best flow)
- Render with client:visible directive: <LeadCaptureForm client:visible lang={lang} />
- The section should have a subtle background differentiation (e.g., slightly different bg via theme variable) to stand out from surrounding sections
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro build 2>&1 | tail -20</automated>
    <manual>Open built homepage and verify lead capture section renders with email input and submit button</manual>
  </verify>
  <done>LeadCaptureForm renders on both homepages with email input, Zod validation, Supabase integration (graceful fallback if env vars missing), and Telegram CTA on success. Build passes.</done>
</task>

</tasks>

<verification>
- `npm ls zod` confirms Zod is installed
- Build passes: `npx astro build`
- LeadCaptureForm component type-checks clean
- Homepage includes lead capture section in both /tl/ and /en/
- Migration SQL is valid and includes RLS
</verification>

<success_criteria>
- Zod validates email input with user-facing error messages
- Form inserts to Supabase leads table (or shows graceful fallback)
- Success state shows Telegram CTA
- Lead capture section visible on both homepages
- Supabase migration includes RLS with INSERT-only anon policy
</success_criteria>

<output>
After completion, create `.planning/phases/02-seo-compliance-affiliates/02-04-SUMMARY.md`
</output>
