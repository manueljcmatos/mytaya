---
phase: 02-seo-compliance-affiliates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/operators.ts
  - src/components/operators/OperatorCard.astro
  - src/components/operators/OperatorGrid.astro
  - src/pages/tl/mga-operator/index.astro
  - src/pages/tl/mga-operator/[slug].astro
  - src/pages/en/operators/index.astro
  - src/pages/en/operators/[slug].astro
autonomous: true
requirements:
  - AFFL-01
  - AFFL-02
  - AFFL-03

must_haves:
  truths:
    - "Operators page displays PAGCOR-licensed operator cards with name, logo placeholder, welcome bonus, and affiliate link"
    - "Each operator has a dedicated review page at /tl/mga-operator/[slug]/ and /en/operators/[slug]/"
    - "All affiliate links use rel='sponsored nofollow noopener' and target='_blank'"
    - "Operator content is bilingual (Filipino and English)"
    - "Operators page shows a brief comparison summary above cards"
  artifacts:
    - path: "src/data/operators.ts"
      provides: "Static operator data with bilingual content"
      exports: ["operators", "Operator"]
    - path: "src/components/operators/OperatorCard.astro"
      provides: "Individual operator card component"
      contains: "rel=\"sponsored"
    - path: "src/pages/en/operators/[slug].astro"
      provides: "Dynamic operator review pages"
      contains: "getStaticPaths"
    - path: "src/pages/tl/mga-operator/[slug].astro"
      provides: "Dynamic operator review pages (Filipino)"
      contains: "getStaticPaths"
  key_links:
    - from: "src/pages/en/operators/index.astro"
      to: "src/data/operators.ts"
      via: "imports operators array for listing"
      pattern: "import.*operators"
    - from: "src/pages/en/operators/[slug].astro"
      to: "src/data/operators.ts"
      via: "imports operators for getStaticPaths and page data"
      pattern: "getStaticPaths"
    - from: "src/components/operators/OperatorCard.astro"
      to: "affiliate URLs"
      via: "rel=sponsored nofollow links"
      pattern: "rel.*sponsored"
---

<objective>
Build the affiliate operators system: static data file, card components, listing page, and individual review pages for PAGCOR-licensed operators.

Purpose: Affiliate operator cards are the primary monetization channel. Each operator gets a dedicated review page for SEO value (long-tail keywords like "BK8 Philippines review"). All affiliate links must use rel="sponsored nofollow" per Google guidelines.
Output: Operator data, card components, listing pages, review pages in both languages
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-seo-compliance-affiliates/02-RESEARCH.md
@src/layouts/Layout.astro
@src/lib/constants.ts
@src/pages/en/operators/index.astro
@src/pages/tl/mga-operator/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create operator data file and card components</name>
  <files>
    src/data/operators.ts
    src/components/operators/OperatorCard.astro
    src/components/operators/OperatorGrid.astro
  </files>
  <action>
Create src/data/operators.ts:
- Export TypeScript interface `Operator` with fields: slug, name, logo (path string in /operators/), welcomeBonus ({tl, en}), description ({tl, en}), review ({tl, en} -- 2-3 paragraphs of review content), affiliateUrl, pagcorLicensed (boolean, all must be true), rating (1-5), features (string[]), sportsCovered (string[])
- Export `operators: Operator[]` with 4-5 placeholder operators. Use well-known PH market operators as examples: BK8, Bet88, 22Bet, 1xBet, Betway. Use realistic but clearly placeholder data (mark affiliate URLs as "#placeholder-affiliate-url" since real URLs need user verification).
- Add a comment at top: "IMPORTANT: Verify PAGCOR licensing status and affiliate URLs before launch"
- Bilingual welcome bonus text, descriptions, and review content for each operator

Create src/components/operators/OperatorCard.astro:
- Props: operator (Operator type), lang ('tl'|'en')
- Rich card layout (per user decision: rich cards, not comparison table)
- Display: operator name, logo placeholder (colored div with initials if no image), welcome bonus, rating (stars), key features (2-3 bullet points), CTA button with affiliate link
- CTA button styled with brand primary color
- ALL affiliate links must have: rel="sponsored nofollow noopener", target="_blank"
- Small "21+" badge on each card (per research discretion recommendation)
- Card uses theme CSS variables (var(--t-*)) for background, text, borders
- Link to individual review page: /{lang route}/operators-or-mga-operator/{slug}/

Create src/components/operators/OperatorGrid.astro:
- Props: operators (Operator[]), lang ('tl'|'en')
- Responsive grid: 1 col mobile, 2 cols md, 3 cols lg
- Maps over operators array and renders OperatorCard for each
- Gap spacing consistent with site design
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx tsc --noEmit src/data/operators.ts 2>&1 | head -10</automated>
    <manual>Review that OperatorCard includes rel="sponsored nofollow noopener" on affiliate links</manual>
  </verify>
  <done>operators.ts exports typed operator data with 4-5 placeholder operators. OperatorCard renders rich cards with proper affiliate link attributes. OperatorGrid provides responsive layout.</done>
</task>

<task type="auto">
  <name>Task 2: Build operator listing and review pages in both languages</name>
  <files>
    src/pages/tl/mga-operator/index.astro
    src/pages/tl/mga-operator/[slug].astro
    src/pages/en/operators/index.astro
    src/pages/en/operators/[slug].astro
  </files>
  <action>
Update existing operator listing pages (src/pages/tl/mga-operator/index.astro and src/pages/en/operators/index.astro):
- Remove EmptyState component, replace with actual operator content
- Import operators from src/data/operators.ts and OperatorGrid component
- Add a brief intro section above the grid: heading + 1-2 sentences explaining these are PAGCOR-licensed operators (bilingual)
- Add a brief comparison summary: simple table or list showing operator name, welcome bonus, rating (above the card grid)
- Pass Organization schema to Layout via schema prop (import buildOrganizationSchema from lib/seo.ts if available, else inline)
- Page title format: "Mga PAGCOR Operator - MyTaya" (tl) / "PAGCOR Licensed Operators - MyTaya" (en)

Create dynamic review pages ([slug].astro) in both language directories:
- Export getStaticPaths() that maps operators array to params: [{params: {slug: op.slug}, props: {operator: op}}]
- Full review page layout with: operator name as h1, rating, welcome bonus callout box, full review text (from operator.review[lang]), features list, sports covered, prominent CTA button with affiliate link (rel="sponsored nofollow noopener")
- Import GamblingDisclaimer from compliance components (full variant) at bottom of each review page
- SEO: descriptive title "{Operator Name} Review - MyTaya", description from first line of review
- hreflang handled automatically by Layout.astro

Both [slug].astro files should be nearly identical except for the lang variable derivation (getLangFromUrl or hardcoded).
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro build 2>&1 | tail -30</automated>
    <manual>Check build output includes /en/operators/bk8/ and /tl/mga-operator/bk8/ (or equivalent slug)</manual>
  </verify>
  <done>Operator listing pages show grid of cards with comparison summary. Each operator has a dedicated review page in both languages with full content, affiliate links (rel=sponsored), and gambling disclaimer.</done>
</task>

</tasks>

<verification>
- Build passes: `npx astro build`
- Operator listing pages render cards (not empty states)
- Review pages generated for each operator slug in both /tl/ and /en/
- All affiliate links have rel="sponsored nofollow noopener"
- 21+ badge visible on operator cards
</verification>

<success_criteria>
- 4-5 operator cards displayed on listing pages in both languages
- Each operator has a review page with bilingual content
- All affiliate links properly attributed with rel="sponsored nofollow noopener"
- Gambling disclaimer appears on review pages
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-seo-compliance-affiliates/02-03-SUMMARY.md`
</output>
