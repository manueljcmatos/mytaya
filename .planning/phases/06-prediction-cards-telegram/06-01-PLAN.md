---
phase: 06-prediction-cards-telegram
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/predictions-worker/src/card-gen.ts
  - workers/predictions-worker/src/card-templates.ts
  - workers/predictions-worker/src/types.ts
  - workers/predictions-worker/src/index.ts
  - workers/predictions-worker/src/resolver.ts
  - workers/predictions-worker/wrangler.toml
  - workers/predictions-worker/package.json
  - supabase/migrations/006_telegram_support.sql
autonomous: true
requirements:
  - CARD-01
  - CARD-02

must_haves:
  truths:
    - "Each new prediction gets a branded 1200x630 PNG card generated and stored in R2"
    - "Each settled prediction gets a result card with final score and WIN/LOSS/PUSH badge"
    - "Card URLs are saved to card_image_url column in predictions table"
  artifacts:
    - path: "workers/predictions-worker/src/card-gen.ts"
      provides: "PNG generation via workers-og, R2 upload, font loading"
      exports: ["generatePredictionCard", "generateResultCard"]
    - path: "workers/predictions-worker/src/card-templates.ts"
      provides: "HTML template builders for prediction and result cards"
      exports: ["buildPredictionCardHtml", "buildResultCardHtml"]
    - path: "supabase/migrations/006_telegram_support.sql"
      provides: "telegram_message_id column on predictions table"
      contains: "ALTER TABLE"
  key_links:
    - from: "workers/predictions-worker/src/index.ts"
      to: "workers/predictions-worker/src/card-gen.ts"
      via: "generatePredictionCard called after prediction upsert"
      pattern: "generatePredictionCard"
    - from: "workers/predictions-worker/src/resolver.ts"
      to: "workers/predictions-worker/src/card-gen.ts"
      via: "generateResultCard called after settlement"
      pattern: "generateResultCard"
    - from: "workers/predictions-worker/src/card-gen.ts"
      to: "env.CARDS_BUCKET"
      via: "R2 put for PNG storage"
      pattern: "CARDS_BUCKET\\.put"
---

<objective>
Generate branded PNG prediction cards via workers-og on the existing predictions-worker, stored in Cloudflare R2.

Purpose: Each prediction needs a shareable 1200x630 card with match details, odds, prediction, and MyTaya branding. Result cards are generated after settlement with final score and WIN/LOSS badge.
Output: card-gen.ts, card-templates.ts, updated pipeline hooks, R2 binding, DB migration
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prediction-cards-telegram/06-RESEARCH.md

@workers/predictions-worker/src/index.ts
@workers/predictions-worker/src/types.ts
@workers/predictions-worker/src/resolver.ts
@workers/predictions-worker/wrangler.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card generation module, templates, and infrastructure</name>
  <files>
    workers/predictions-worker/src/card-gen.ts
    workers/predictions-worker/src/card-templates.ts
    workers/predictions-worker/src/types.ts
    workers/predictions-worker/wrangler.toml
    workers/predictions-worker/package.json
    supabase/migrations/006_telegram_support.sql
  </files>
  <action>
    1. Install workers-og in the predictions-worker: `cd workers/predictions-worker && npm install workers-og`

    2. Create `supabase/migrations/006_telegram_support.sql`:
       - ALTER TABLE public.predictions ADD COLUMN IF NOT EXISTS telegram_message_id BIGINT;

    3. Update `types.ts` Env interface to add:
       - CARDS_BUCKET: R2Bucket (R2 binding)
       - TELEGRAM_BOT_TOKEN: string
       - TELEGRAM_CHANNEL_ID: string
       Keep all existing fields unchanged.

    4. Update `wrangler.toml` to add R2 bucket binding:
       ```toml
       [[r2_buckets]]
       binding = "CARDS_BUCKET"
       bucket_name = "mytaya-cards"
       preview_bucket_name = "mytaya-cards-preview"
       ```
       Add comments for new secrets: TELEGRAM_BOT_TOKEN, TELEGRAM_CHANNEL_ID

    5. Create `card-templates.ts` with two exported functions:

       `buildPredictionCardHtml(data)` - Takes prediction data (homeTeam, awayTeam, league, matchDate, pick, odds, confidence, sport) and returns HTML string for Satori. Layout:
       - 1200x630px, dark background (#0A0A0A), white text
       - Top bar: MY (teal #0F766E) + TAYA wordmark (Bebas Neue 48px), league name right-aligned
       - Center: team names large (Bebas Neue 56px) with VS separator
       - Bottom accent bar: sport-themed color (football=#0F766E green, basketball=#f97316 orange) with pick, odds, confidence
       - Footer: mytaya.com + match date/time
       - All styles inline flexbox (Satori CSS subset)

       `buildResultCardHtml(data)` - Takes result data (homeTeam, awayTeam, homeScore, awayScore, pick, result, sport) and returns HTML string. Layout:
       - Same header as prediction card
       - Center: team names with large score numbers (Bebas Neue 72px)
       - Large WIN/LOSS/PUSH badge: win=#22C55E, loss=#EF4444, push=#F59E0B
       - Border and text color match result

    6. Create `card-gen.ts` with:

       `loadFont(env: Env, fontFile: string): Promise<ArrayBuffer>` - Loads TTF font from R2 bucket at `fonts/{fontFile}`. Fonts must be uploaded to R2 manually (Bebas Neue Regular TTF, Inter Regular TTF).

       `generatePredictionCard(env: Env, prediction: PredictionCardData): Promise<string>` - Uses workers-og ImageResponse with the prediction template HTML, 1200x630 dimensions, Bebas Neue + Inter fonts. Uploads PNG to R2 at `cards/{slug}.png`. Updates predictions table card_image_url. Returns the public R2 URL.

       `generateResultCard(env: Env, prediction: ResultCardData): Promise<string>` - Same pattern but uses result template. Uploads to `cards/{slug}-result.png`. Updates card_image_url to point to result card (overwrites prediction card URL since result is the final state).

       Define PredictionCardData and ResultCardData interfaces in card-gen.ts (or types.ts) with the fields needed by the templates.

       Use `https://{R2_PUBLIC_DOMAIN}/cards/{slug}.png` as the URL pattern. For now use a placeholder constant like `https://cards.mytaya.com` -- the user will configure R2 public access.

       Wrap each card generation in try/catch so pipeline continues if card gen fails (card gen is enhancement, not critical path).
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya/workers/predictions-worker && npx tsc --noEmit</automated>
    <manual>Check that card-gen.ts imports from workers-og, card-templates.ts exports both HTML builders, types.ts has CARDS_BUCKET</manual>
  </verify>
  <done>card-gen.ts and card-templates.ts exist with typed exports, workers-og installed, R2 binding configured in wrangler.toml, migration file ready, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Hook card generation into prediction and resolution pipelines</name>
  <files>
    workers/predictions-worker/src/index.ts
    workers/predictions-worker/src/resolver.ts
  </files>
  <action>
    1. In `index.ts`, import `generatePredictionCard` from `./card-gen`.

    2. In `fetchAndGeneratePredictions()`, after each successful prediction upsert (the `else` branch after `insertError` check), add a card generation call:
       - Query the just-inserted prediction to get its id (use the slug to select)
       - Build PredictionCardData from the fixture + prediction data available in scope (homeTeam name, awayTeam name, league name, matchDate, pick_label_en, odds, confidence, sport)
       - Call `generatePredictionCard(env, cardData)` wrapped in try/catch
       - Log success/failure but do NOT fail the prediction pipeline if card gen fails

    3. Apply the same pattern in `fetchAndGenerateNbaPredictions()` -- after each successful NBA prediction upsert, generate a prediction card with sport='basketball'.

    4. In `resolver.ts`, import `generateResultCard` from `./card-gen`.

    5. In `resolveFinishedMatches()`, after each successful settlement update (the `else` branch after `updateError` check), add result card generation:
       - Need homeTeam name and awayTeam name -- extend the initial query to join teams table: `.select('id, api_fixture_id, pick, slug, home_team:teams!predictions_home_team_id_fkey(name), away_team:teams!predictions_away_team_id_fkey(name)')`
       - Build ResultCardData from pred + fixture result (homeScore, awayScore, result, sport)
       - Call `generateResultCard(env, resultCardData)` wrapped in try/catch
       - Log success/failure

    6. Apply the same pattern in `resolveNbaMatches()` -- after each NBA settlement, generate a result card.

    Note: The resolver functions use `ctx.waitUntil` in index.ts, so they already run asynchronously. Card generation within them is fine.

    IMPORTANT: Do not change the existing prediction generation logic, odds handling, or resolution logic. Only ADD card generation calls after existing success paths.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya/workers/predictions-worker && npx tsc --noEmit</automated>
    <manual>Verify index.ts calls generatePredictionCard after upsert, resolver.ts calls generateResultCard after settlement</manual>
  </verify>
  <done>Prediction cards generate after each prediction insert (football + NBA), result cards generate after each settlement (football + NBA), all wrapped in try/catch so pipelines are not disrupted</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `cd workers/predictions-worker && npx tsc --noEmit`
- workers-og is in package.json dependencies
- wrangler.toml has R2 bucket binding CARDS_BUCKET
- card-templates.ts exports buildPredictionCardHtml and buildResultCardHtml
- card-gen.ts exports generatePredictionCard and generateResultCard
- index.ts imports and calls generatePredictionCard after prediction upsert
- resolver.ts imports and calls generateResultCard after settlement
- Migration file 006_telegram_support.sql exists with telegram_message_id column
</verification>

<success_criteria>
- `npx tsc --noEmit` passes in predictions-worker
- Both card template functions produce valid HTML strings with 1200x630 layout
- Card generation is hooked into all 4 pipeline paths (football predict, NBA predict, football resolve, NBA resolve)
- All card gen calls are wrapped in try/catch to not disrupt existing pipelines
- R2 bucket binding and new env vars declared in wrangler.toml and types.ts
</success_criteria>

<output>
After completion, create `.planning/phases/06-prediction-cards-telegram/06-01-SUMMARY.md`
</output>
