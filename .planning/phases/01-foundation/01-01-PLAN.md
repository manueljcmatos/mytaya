---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - src/styles/global.css
  - src/i18n/ui.ts
  - src/i18n/utils.ts
  - src/i18n/routes.ts
  - src/lib/supabase.ts
  - src/lib/constants.ts
  - supabase/migrations/00000000000000_foundation.sql
  - .env.example
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - FOUND-07
user_setup:
  - service: supabase
    why: "Database for predictions, posts, stats, leads"
    env_vars:
      - name: PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> Project API keys -> anon/public"

must_haves:
  truths:
    - "Astro dev server starts without errors"
    - "Tailwind v4 styles compile correctly via @tailwindcss/vite"
    - "i18n routing config produces /tl/ and /en/ URL prefixes"
    - "CSS custom properties define dark (default) and light theme tokens"
    - "Supabase migration creates predictions table with bilingual columns and odds fields"
    - "PWA manifest is configured with Philippine-themed metadata"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro config with i18n, React, sitemap, PWA integrations"
      contains: "prefixDefaultLocale: true"
    - path: "src/styles/global.css"
      provides: "Tailwind v4 imports, @theme brand tokens, dark/light CSS custom properties"
      contains: "@import \"tailwindcss\""
    - path: "src/i18n/ui.ts"
      provides: "UI string translations for tl and en"
      exports: ["ui", "languages", "defaultLang"]
    - path: "src/i18n/utils.ts"
      provides: "Translation helper functions"
      exports: ["getLangFromUrl", "useTranslations"]
    - path: "src/i18n/routes.ts"
      provides: "Localized route name mapping for language switcher"
      exports: ["getAlternatePath"]
    - path: "src/lib/supabase.ts"
      provides: "Supabase client for build-time data fetching"
      exports: ["supabase"]
    - path: "supabase/migrations/00000000000000_foundation.sql"
      provides: "Foundation schema with leagues, teams, predictions, posts, prediction_stats, leads tables"
      contains: "pick_label_tl"
  key_links:
    - from: "astro.config.mjs"
      to: "src/styles/global.css"
      via: "@tailwindcss/vite plugin in vite.plugins"
      pattern: "tailwindcss\\(\\)"
    - from: "src/i18n/utils.ts"
      to: "src/i18n/ui.ts"
      via: "import of ui and defaultLang"
      pattern: "import.*from.*\\.\\./i18n/ui"
---

<objective>
Initialize the Astro 5 project with all dependencies, i18n routing configuration, Tailwind v4 theme system with dark/light CSS custom properties, i18n translation helpers, Supabase client, and the foundation database migration.

Purpose: This is the project skeleton that every subsequent plan and phase builds upon. Getting the config, theming, i18n infrastructure, and database schema right from day one prevents cascading rework.
Output: A buildable Astro project with all integrations configured, plus the Supabase migration SQL ready to apply.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Reference project for architecture patterns (same stack, different visual identity)
@/Users/mjcmatos/Desktop/betpro/astro.config.mjs
@/Users/mjcmatos/Desktop/betpro/package.json
@/Users/mjcmatos/Desktop/betpro/src/styles/global.css
@/Users/mjcmatos/Desktop/betpro/src/lib/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project with all integrations, i18n config, theme system, and translation helpers</name>
  <files>
    package.json
    astro.config.mjs
    tsconfig.json
    src/styles/global.css
    src/i18n/ui.ts
    src/i18n/utils.ts
    src/i18n/routes.ts
    src/lib/supabase.ts
    src/lib/constants.ts
    src/env.d.ts
    .env.example
    public/favicon.svg
  </files>
  <action>
    1. Create Astro 5 project using `npm create astro@latest . -- --template minimal --no-install --typescript strict` in the mytaya directory (the project root already exists as a git repo with .planning/).

    2. Install dependencies:
       ```
       npm install astro @astrojs/react @astrojs/sitemap @supabase/supabase-js react react-dom
       npm install tailwindcss @tailwindcss/vite @tailwindcss/typography
       npm install -D @vite-pwa/astro @types/react @types/react-dom
       ```

    3. Configure `astro.config.mjs` following the RESEARCH.md Pattern 1:
       - site: 'https://mytaya.com'
       - i18n: locales ['tl', 'en'], defaultLocale 'tl', fallback { tl: 'en' }, routing with prefixDefaultLocale: true, redirectToDefaultLocale: false
       - integrations: react(), sitemap({ i18n: { defaultLocale: 'tl', locales: { tl: 'fil-PH', en: 'en-PH' } } }), AstroPWA (see RESEARCH.md Pattern 1 for full config)
       - vite.plugins: [tailwindcss()]
       - Do NOT use @astrojs/tailwind (does not support Tailwind v4). Use @tailwindcss/vite as Vite plugin.

    4. Create `src/styles/global.css` following RESEARCH.md Pattern 2 (CSS custom properties theme system):
       - @import "tailwindcss" and @plugin "@tailwindcss/typography"
       - @custom-variant dark (&:where(.dark, .dark *));
       - @theme block with brand tokens. Choose a FRESH color palette distinct from betpro's gold (#C8A84E):
         * Use a vibrant Philippine-inspired accent (e.g., royal blue #1E40AF or deep teal #0D9488 or tropical green -- Claude's discretion)
         * --color-brand-primary, --color-brand-light, --color-win (#22C55E), --color-loss (#EF4444)
         * --font-display and --font-body: choose fonts DIFFERENT from betpro (Oswald/Outfit). Use bold condensed sans-serif for display and modern geometric sans for body. Load via Google Fonts link in Layout (Plan 02).
       - :root (dark theme default) with --t-bg, --t-bg-card, --t-border, --t-text, --t-text-sec, --t-header-bg, etc.
       - :root.light with light counterparts
       - Body base styles (font-family, background, color, min-height)
       - Heading styles (font-family: display)
       - Reusable CSS classes: .btn-primary, .btn-outline, .btn-ghost, .card, .card-hover, .nav-link, .footer-link, .glass (follow betpro's pattern but use new brand colors)
       - Prose overrides for blog content (like betpro's .prose rules)

    5. Create `src/i18n/ui.ts` with UI string translations following RESEARCH.md Pattern 4:
       - languages, defaultLang exports
       - ui object with tl and en keys
       - Include translations for: nav items (Predictions/Mga Hula, Results/Mga Resulta, Blog, Operators/Mga Operator), hero text, CTAs (Telegram, Today's Predictions), theme toggle label, footer disclaimer, empty states for predictions/results/blog/operators, sport names (Basketball/Basketbol, Football/Futbol, Boxing/Boksing), common labels (Today/Ngayon, View All/Tingnan Lahat, etc.)

    6. Create `src/i18n/utils.ts` with getLangFromUrl() and useTranslations() following RESEARCH.md Pattern 4.

    7. Create `src/i18n/routes.ts` with route mapping following RESEARCH.md code example:
       - Map: / <-> /, /hula/ <-> /predictions/, /resulta/ <-> /results/, /blog/ <-> /blog/, /mga-operator/ <-> /operators/
       - Export getAlternatePath() function

    8. Create `src/lib/supabase.ts` following betpro's pattern:
       - Import from @supabase/supabase-js
       - Read PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY from import.meta.env
       - Conditional client creation (null if not configured)
       - No hardcoded fallback keys (unlike betpro -- this is a new project)

    9. Create `src/lib/constants.ts` with:
       - Sports enum/array: ['basketball', 'football', 'boxing']
       - Nav items array (used by Header)
       - Social links (Telegram channel URL placeholder)
       - Site metadata constants

    10. Create `.env.example` with PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY placeholders.

    11. Create a simple `public/favicon.svg` -- a minimal SVG placeholder with the brand accent color (will be refined later).

    12. Update `src/env.d.ts` to include Astro client types and PWA virtual module declarations.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npm run build 2>&1 | tail -20</automated>
    <manual>Check that astro.config.mjs has i18n config, global.css has @theme block and theme vars, i18n/ui.ts has both tl and en translations</manual>
  </verify>
  <done>
    - `npm run build` succeeds (or `npm run dev` starts without config errors)
    - astro.config.mjs has i18n with prefixDefaultLocale: true, React, sitemap with i18n, PWA, Tailwind vite plugin
    - global.css has @import "tailwindcss", @theme block with brand tokens, :root dark vars, :root.light light vars
    - i18n/ui.ts has complete translations for both locales
    - i18n/utils.ts exports getLangFromUrl and useTranslations
    - i18n/routes.ts exports getAlternatePath with route mappings
    - supabase.ts creates conditional client from env vars
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Supabase foundation migration with bilingual predictions schema</name>
  <files>
    supabase/migrations/00000000000000_foundation.sql
    supabase/config.toml
  </files>
  <action>
    1. Create `supabase/config.toml` with basic Supabase CLI config (project_id placeholder).

    2. Create `supabase/migrations/00000000000000_foundation.sql` following the RESEARCH.md schema exactly:
       - leagues table: id (UUID PK), name, slug (unique), sport (CHECK basketball/football/boxing), country, api_source (CHECK api-sports/betsapi/manual), api_league_id, logo_url, is_active, created_at
       - teams table: id (UUID PK), name, slug (unique), league_id (FK -> leagues), api_team_id, logo_url, created_at
       - predictions table (FOUND-04 + FOUND-05): id (UUID PK), slug (unique), league_id (FK), home_team_id (FK), away_team_id (FK), match_date (TIMESTAMPTZ),
         * Bilingual columns: pick, pick_label_tl, pick_label_en, analysis_tl, analysis_en
         * Odds tracking: odds (DECIMAL 6,2), odds_source, stake (1-10), confidence (high/medium/low)
         * Resolution: result (win/loss/push/void), status (pending/settled/cancelled), settled_at
         * Publishing: published_site, published_telegram, card_image_url
         * API tracking: api_fixture_id
         * created_at
       - posts table: id, slug (unique), title_tl, title_en, content_tl, content_en, excerpt_tl, excerpt_en, category, sport, featured_image_url, is_published, published_at, created_at
       - prediction_stats table: id, sport, period, period_start, total_picks, wins, losses, pushes, win_rate, roi, streak, updated_at
       - leads table: id, name, email, phone, site_origin (default 'mytaya'), created_at
       - Enable RLS on ALL tables
       - Create public read policies for leagues, teams, published predictions, published posts, stats
       - Create anon insert policy for leads (no read)
       - Create performance indexes: predictions by match_date DESC, status, published_site+created_at; posts by is_published+published_at; stats by sport+period

    NOTE: The index `idx_predictions_sport` from RESEARCH.md uses a subquery which is invalid in a btree index. Instead, add a `sport` column directly to predictions table (denormalized from league for query performance), OR create a simple index on league_id and join at query time. Prefer adding a `sport` column to predictions for simpler queries -- add CHECK constraint matching leagues.sport values.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && cat supabase/migrations/00000000000000_foundation.sql | grep -c "CREATE TABLE" && echo "tables created" && cat supabase/migrations/00000000000000_foundation.sql | grep -c "pick_label_tl" && echo "bilingual columns present" && cat supabase/migrations/00000000000000_foundation.sql | grep -c "ENABLE ROW LEVEL SECURITY" && echo "RLS enabled"</automated>
  </verify>
  <done>
    - Migration file creates 6 tables: leagues, teams, predictions, posts, prediction_stats, leads
    - predictions table has bilingual columns (pick_label_tl/en, analysis_tl/en) satisfying FOUND-04
    - predictions table has odds DECIMAL column satisfying FOUND-05
    - RLS enabled on all tables with appropriate read/insert policies
    - Performance indexes created for common query patterns
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -r "prefixDefaultLocale" astro.config.mjs` returns a match
3. `grep -c "@theme" src/styles/global.css` returns >= 1
4. `grep -c "pick_label_tl" supabase/migrations/*.sql` returns >= 1
5. `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/*.sql` returns 6
</verification>

<success_criteria>
- Astro 5 project builds successfully with all integrations (React, sitemap, PWA, Tailwind v4)
- i18n routing configured for /tl/ and /en/ prefixes with Filipino as default
- Theme system uses CSS custom properties with dark default and light alternative
- Brand colors are distinctly different from betpro's gold scheme
- Supabase migration defines complete schema with bilingual columns and odds tracking
- All i18n helpers (ui.ts, utils.ts, routes.ts) are complete and importable
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
