---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/layouts/Layout.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/LanguageSwitcher.astro
  - src/components/ThemeToggle.astro
  - src/pages/index.astro
autonomous: true
requirements:
  - FOUND-02
  - FOUND-06
  - FOUND-08

must_haves:
  truths:
    - "Every page has hreflang tags pointing to both /tl/ and /en/ versions"
    - "User can click flag icon to switch between Filipino and English"
    - "User can toggle dark/light theme and preference persists across page loads"
    - "No flash of wrong theme on page load (FOUC prevention)"
    - "Header shows nav links, language switcher, theme toggle, and Telegram CTA"
    - "Footer shows brand, navigation links, and gambling disclaimer"
    - "Mobile menu opens/closes with hamburger button"
  artifacts:
    - path: "src/layouts/Layout.astro"
      provides: "Base layout with hreflang, OG meta, FOUC prevention script, PWA registration, Google Fonts"
      contains: "hreflang"
      min_lines: 50
    - path: "src/components/Header.astro"
      provides: "Site header with desktop nav, mobile menu, language switcher, theme toggle, Telegram CTA"
      min_lines: 60
    - path: "src/components/Footer.astro"
      provides: "Site footer with brand, nav links, disclaimer"
      min_lines: 30
    - path: "src/components/LanguageSwitcher.astro"
      provides: "Flag-based language toggle using i18n route mapping"
      contains: "getAlternatePath"
    - path: "src/components/ThemeToggle.astro"
      provides: "Sun/moon theme toggle button with localStorage persistence"
      contains: "localStorage"
    - path: "src/pages/index.astro"
      provides: "Root redirect to /tl/ or /en/ based on browser language"
      contains: "Astro.preferredLocale"
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "src/styles/global.css"
      via: "import statement"
      pattern: "import.*styles/global\\.css"
    - from: "src/layouts/Layout.astro"
      to: "src/components/Header.astro"
      via: "component import and render"
      pattern: "import Header"
    - from: "src/components/LanguageSwitcher.astro"
      to: "src/i18n/routes.ts"
      via: "getAlternatePath import for URL construction"
      pattern: "getAlternatePath"
    - from: "src/components/Header.astro"
      to: "src/i18n/utils.ts"
      via: "useTranslations for nav labels"
      pattern: "useTranslations"
---

<objective>
Build the base Layout, Header, Footer, Language Switcher, and Theme Toggle components that wrap every page on the site. Also create the root index.astro that redirects to the correct locale.

Purpose: These are the shell components every page uses. The layout provides SEO tags, FOUC prevention, and PWA registration. The header provides navigation, language switching, and theme toggling. Getting these right means every subsequent page automatically inherits correct behavior.
Output: Complete site shell that can render any page with proper hreflang, theme, navigation, and language switching.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

# Reference for Header/Footer/Layout patterns
@/Users/mjcmatos/Desktop/betpro/src/layouts/Layout.astro
@/Users/mjcmatos/Desktop/betpro/src/components/Header.astro
@/Users/mjcmatos/Desktop/betpro/src/components/Footer.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Layout.astro with hreflang, FOUC prevention, OG meta, and PWA registration</name>
  <files>
    src/layouts/Layout.astro
    src/pages/index.astro
  </files>
  <action>
    1. Create `src/layouts/Layout.astro` following betpro's Layout pattern + RESEARCH.md patterns 5 and 6:

    Props interface: title, description (with Filipino default), ogImage, ogType, schema (for JSON-LD), noindex, lang (extracted from URL or passed).

    In the head:
    - charset, viewport meta tags
    - Description meta tag
    - Canonical URL (derived from Astro.url + Astro.site)
    - Hreflang tags (RESEARCH.md Pattern 5): `<link rel="alternate" hreflang="fil" href={tlUrl} />` and `<link rel="alternate" hreflang="en" href={enUrl} />` and `<link rel="alternate" hreflang="x-default" href={enUrl} />`. Use `getAbsoluteLocaleUrl` from 'astro:i18n'. Extract path without locale prefix, construct both locale URLs.
    - Set `<html lang=...>` dynamically: 'fil-PH' for tl locale, 'en-PH' for en locale
    - Favicon link
    - OG tags (og:type, og:url, og:title, og:description, og:image with absolute URL, og:locale)
    - Twitter card tags (summary_large_image)
    - Google Fonts link for the display + body fonts chosen in Plan 01's global.css
    - Theme color meta tag using brand primary color
    - Apple touch icon
    - FOUC prevention inline script (RESEARCH.md Pattern 6): reads localStorage('theme'), adds .light class if needed, BEFORE any stylesheet
    - Import global.css
    - JSON-LD schema slot (if schema prop provided)
    - `<slot name="head" />` for page-specific head content
    - PWA info link tag: `import { pwaInfo } from 'virtual:pwa-info'` and render webManifest linkTag

    In body:
    - `<Header />`
    - `<main class="flex-1"><slot /></main>`
    - `<Footer />`
    - PWA registration script: `import { registerSW } from 'virtual:pwa-register'; registerSW({ immediate: true });`

    2. Create `src/pages/index.astro` (root redirect) following RESEARCH.md Pattern 3:
    - In frontmatter: `const preferred = Astro.preferredLocale || 'tl'; const target = preferred === 'en' ? '/en/' : '/tl/'; return Astro.redirect(target, 302);`
    - No HTML body needed (redirect happens in frontmatter)
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && grep -c "hreflang" src/layouts/Layout.astro && grep -c "localStorage" src/layouts/Layout.astro && grep -c "preferredLocale" src/pages/index.astro</automated>
  </verify>
  <done>
    - Layout.astro renders correct hreflang tags for both locales
    - FOUC prevention script appears in head before stylesheets
    - HTML lang attribute is dynamic based on locale
    - OG and Twitter meta tags are present
    - PWA manifest link and registration script are included
    - Root index.astro redirects to /tl/ or /en/ based on browser language
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Header, Footer, LanguageSwitcher, and ThemeToggle components</name>
  <files>
    src/components/Header.astro
    src/components/Footer.astro
    src/components/LanguageSwitcher.astro
    src/components/ThemeToggle.astro
  </files>
  <action>
    1. Create `src/components/ThemeToggle.astro`:
       - Sun icon (visible in dark mode) and moon icon (visible in light mode) using inline SVG
       - Button with aria-label from translations (t('theme.toggle'))
       - Styled with theme vars: color var(--t-text-sec), border var(--t-border)
       - Script section (not is:inline -- uses Astro's script bundling):
         * toggleTheme function: toggle .light class on documentElement, save to localStorage
         * updateIcons function: show/hide sun/moon based on .light class presence
         * Attach click listener, call updateIcons on load
       - Accept lang prop for translated aria-label

    2. Create `src/components/LanguageSwitcher.astro` following RESEARCH.md code example:
       - Import getLangFromUrl from i18n/utils, getAlternatePath from i18n/routes, getRelativeLocaleUrl from 'astro:i18n'
       - Determine current lang, compute alternate lang URL using getAlternatePath + getRelativeLocaleUrl
       - Render an <a> tag with flag image + locale code label
       - Use inline SVG or emoji flags for PH (for Filipino/tl) and US (for English/en). Prefer small SVG flag icons. If using image files, create placeholder SVGs at public/flags/ph.svg and public/flags/us.svg.
       - Style: flex items-center gap, rounded border with theme vars

    3. Create `src/components/Header.astro` following betpro's Header pattern but with MyTaya branding:
       - Import LanguageSwitcher, ThemeToggle, getLangFromUrl, useTranslations
       - Get current lang and t() helper
       - Nav links array from t() translations: Predictions (Mga Hula), Results (Mga Resulta), Blog, Operators (Mga Operator)
       - Map nav links to localized URLs (e.g., /tl/hula/, /en/predictions/)
       - Desktop layout: Logo/wordmark left, nav center, [LanguageSwitcher + ThemeToggle + Telegram CTA] right
       - Logo/wordmark: "MY" in brand primary + "TAYA" in theme text color (using display font). Or Claude's discretion on wordmark design -- must feel local/casual per user decision.
       - Mobile layout: Logo left, [ThemeToggle + LanguageSwitcher + hamburger] right
       - Mobile menu (hidden by default): full nav links + Telegram CTA, revealed on hamburger click
       - Telegram CTA: Telegram icon SVG + translated text, styled with brand primary color
       - Sticky header with glass blur effect (like betpro)
       - Script for mobile menu toggle (click hamburger -> toggle hidden class, swap open/close icons)

    4. Create `src/components/Footer.astro` following betpro's Footer pattern but with MyTaya content:
       - Import getLangFromUrl, useTranslations
       - 3-column grid (2 on mobile): Brand/description, Predictions links, Resources links
       - Brand column: MyTaya wordmark + short description in current locale
       - Predictions column: links to sports predictions pages (localized URLs)
       - Resources column: links to operators, blog, responsible gambling (placeholder hrefs)
       - Bottom bar: copyright with current year, gambling disclaimer from t(), 18+ age notice
       - All text uses theme CSS vars, links use .footer-link class
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && grep -c "getAlternatePath" src/components/LanguageSwitcher.astro && grep -c "localStorage" src/components/ThemeToggle.astro && grep -c "mobile-menu" src/components/Header.astro && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Header shows nav links with localized labels and URLs
    - LanguageSwitcher links to alternate locale version of current page using flag icons
    - ThemeToggle persists preference to localStorage and updates sun/moon icons
    - Footer renders brand, nav links, and gambling disclaimer in correct locale
    - Mobile menu toggles on hamburger click
    - `npm run build` succeeds with all components
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Layout.astro has 3 hreflang link tags (fil, en, x-default)
3. Header has desktop nav, mobile menu, language switcher, theme toggle, Telegram CTA
4. LanguageSwitcher uses getAlternatePath for correct cross-locale URL mapping
5. ThemeToggle uses localStorage for persistence and has FOUC prevention in Layout
6. Footer has gambling disclaimer and copyright
7. Root index.astro redirects based on Astro.preferredLocale
</verification>

<success_criteria>
- Every page wrapped in Layout gets hreflang tags, OG meta, theme FOUC prevention, and PWA registration
- Language switcher correctly maps between localized route names (/hula/ <-> /predictions/)
- Theme toggle switches between dark/light, persists in localStorage, no flash on reload
- Header navigation works on both desktop (horizontal nav) and mobile (hamburger menu)
- Telegram CTA is prominently visible in the header
- Footer includes bilingual gambling disclaimer
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
