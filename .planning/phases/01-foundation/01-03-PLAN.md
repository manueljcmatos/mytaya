---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/pages/tl/index.astro
  - src/pages/en/index.astro
  - src/pages/tl/hula/index.astro
  - src/pages/en/predictions/index.astro
  - src/pages/tl/resulta/index.astro
  - src/pages/en/results/index.astro
  - src/pages/tl/blog/index.astro
  - src/pages/en/blog/index.astro
  - src/pages/tl/mga-operator/index.astro
  - src/pages/en/operators/index.astro
  - src/components/EmptyState.astro
  - src/components/SportTabs.astro
  - src/components/TelegramBanner.astro
  - src/components/HeroSection.astro
autonomous: false
requirements:
  - FOUND-01
  - FOUND-07
  - FOUND-08

must_haves:
  truths:
    - "User visiting /tl/ sees the homepage in Filipino"
    - "User visiting /en/ sees the homepage in English"
    - "Homepage has hero section, sport tabs, empty-state placeholder sections for predictions/results/blog/operators"
    - "Telegram CTA is prominently placed on the homepage"
    - "All sub-pages (predictions, results, blog, operators) render with empty states in both locales"
    - "Site is installable as PWA from browser"
    - "Visual identity is distinctly Philippine-inspired and NOT a betpro clone"
  artifacts:
    - path: "src/pages/tl/index.astro"
      provides: "Filipino homepage with hero, sport tabs, empty sections, Telegram CTA"
      min_lines: 30
    - path: "src/pages/en/index.astro"
      provides: "English homepage mirroring Filipino structure"
      min_lines: 30
    - path: "src/components/HeroSection.astro"
      provides: "Hero with headline, subtitle, CTA buttons"
      min_lines: 20
    - path: "src/components/SportTabs.astro"
      provides: "Basketball | Football | Boxing tab navigation"
      contains: "basketball"
    - path: "src/components/EmptyState.astro"
      provides: "Reusable empty state placeholder with icon and message"
      min_lines: 10
    - path: "src/components/TelegramBanner.astro"
      provides: "CTA banner to join Telegram group"
      min_lines: 10
  key_links:
    - from: "src/pages/tl/index.astro"
      to: "src/layouts/Layout.astro"
      via: "layout wrapper"
      pattern: "import Layout"
    - from: "src/pages/tl/index.astro"
      to: "src/i18n/utils.ts"
      via: "useTranslations for page content"
      pattern: "useTranslations"
    - from: "src/components/SportTabs.astro"
      to: "src/lib/constants.ts"
      via: "sports array import"
      pattern: "import.*constants"
---

<objective>
Create the bilingual homepage and all section landing pages with empty-state placeholders. Build reusable components (HeroSection, SportTabs, TelegramBanner, EmptyState) and verify the complete site shell with a human checkpoint.

Purpose: This delivers the visible, browsable site that users will see. Every page renders in both languages, uses the theme system, and demonstrates the Philippine-inspired visual identity. The checkpoint ensures the design and functionality meet expectations before building on top.
Output: Complete browsable site shell in both Filipino and English with all section pages.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build homepage and all section pages with reusable components</name>
  <files>
    src/components/HeroSection.astro
    src/components/SportTabs.astro
    src/components/TelegramBanner.astro
    src/components/EmptyState.astro
    src/pages/tl/index.astro
    src/pages/en/index.astro
    src/pages/tl/hula/index.astro
    src/pages/en/predictions/index.astro
    src/pages/tl/resulta/index.astro
    src/pages/en/results/index.astro
    src/pages/tl/blog/index.astro
    src/pages/en/blog/index.astro
    src/pages/tl/mga-operator/index.astro
    src/pages/en/operators/index.astro
  </files>
  <action>
    1. Create `src/components/HeroSection.astro`:
       - Accept lang prop, use useTranslations(lang) for all text
       - Large headline using display font with brand gradient or accent color
       - Subtitle: "Basketball, Football, at Boxing para sa Filipino" (localized)
       - Two CTA buttons: "Mga Hula Ngayon" / "Today's Predictions" (btn-primary, links to predictions page) and "Sumali sa Telegram" / "Join Telegram" (btn-outline, links to Telegram)
       - Background: subtle gradient or pattern that feels Philippine-inspired (Claude's discretion)
       - Responsive: large text on desktop, scaled down on mobile
       - Visual must feel bold, sporty, and credible -- NOT corporate

    2. Create `src/components/SportTabs.astro`:
       - Accept lang and activeSport props
       - Three tabs: Basketball (Basketbol) | Football (Futbol) | Boxing (Boksing)
       - Each tab links to the sport-specific prediction page (placeholder hrefs for now, e.g., /tl/hula/?sport=basketball)
       - Active tab styled with brand primary color, inactive with muted style
       - Icons for each sport (inline SVG or emoji: basketball, football, boxing glove)
       - Horizontal scrollable on mobile if needed

    3. Create `src/components/TelegramBanner.astro`:
       - Accept lang prop
       - Prominent CTA section with Telegram icon, headline "Join our Telegram channel" (localized), brief description, and btn-primary link
       - Card-style with brand accent border or subtle glow
       - Per user decision: this is the PRIMARY CTA -- make it visually prominent

    4. Create `src/components/EmptyState.astro`:
       - Accept props: message (string), icon (optional slot or default)
       - Center-aligned with muted icon, message text in --t-text-sec
       - Default icon: a subtle placeholder graphic (calendar, chart, etc.)
       - Used for sections that will be populated in later phases

    5. Create homepage pages (`src/pages/tl/index.astro` and `src/pages/en/index.astro`):
       - Each wraps content in Layout with localized title and description
       - Extract lang from URL using getLangFromUrl
       - Sections in order (per user decision):
         a. HeroSection
         b. SportTabs (no active sport on homepage)
         c. "Today's Predictions" section header + EmptyState (message: t('empty.predictions'))
         d. "Recent Results" section header + EmptyState (message: t('empty.results'))
         e. TelegramBanner (prominent placement between content sections)
         f. "Latest Blog Posts" section header + EmptyState (message: t('empty.blog'))
         g. "Operators" section header + EmptyState (message: t('empty.operators'))
       - Each section has a heading and a "View All" link to the respective page
       - Use max-w-7xl mx-auto container with responsive padding
       - Sections separated with spacing and subtle dividers

    6. Create sub-pages (8 pages total, 4 per locale):
       - Predictions: src/pages/tl/hula/index.astro and src/pages/en/predictions/index.astro
         * Layout with localized title "Mga Hula" / "Predictions"
         * SportTabs component (no active sport)
         * EmptyState with predictions-specific message
       - Results: src/pages/tl/resulta/index.astro and src/pages/en/results/index.astro
         * Layout with localized title "Mga Resulta" / "Results"
         * EmptyState with results-specific message
       - Blog: src/pages/tl/blog/index.astro and src/pages/en/blog/index.astro
         * Layout with localized title "Blog"
         * EmptyState with blog-specific message
       - Operators: src/pages/tl/mga-operator/index.astro and src/pages/en/operators/index.astro
         * Layout with localized title "Mga Operator" / "Operators"
         * EmptyState with operators-specific message

    All sub-pages follow the same pattern: import Layout, getLangFromUrl, useTranslations; wrap in Layout; render page-specific heading + content.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npm run build 2>&1 | tail -10 && echo "---" && ls dist/tl/index.html dist/en/index.html dist/tl/hula/index.html dist/en/predictions/index.html 2>&1</automated>
    <manual>Start dev server with `npm run dev` and verify pages load at /tl/ and /en/</manual>
  </verify>
  <done>
    - Homepage renders in both /tl/ and /en/ with hero, sport tabs, empty sections, Telegram CTA
    - All 4 sub-pages exist in both locales (8 pages total)
    - EmptyState component renders placeholder messages from translations
    - SportTabs shows Basketball | Football | Boxing with localized labels
    - TelegramBanner is visually prominent
    - `npm run build` generates all pages in dist/
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify visual identity, i18n, theme, and PWA</name>
  <files>no files modified -- verification only</files>
  <action>
    Start the dev server and present the site to the user for visual and functional inspection. This checkpoint covers the entire Phase 1 deliverable: visual identity, bilingual routing, theme toggle, PWA, and page completeness.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User approves the visual identity, language switching, theme toggle, and overall site shell</done>
  <what-built>Complete bilingual site shell with Philippine-inspired visual identity, dark/light theme toggle, language switcher, PWA support, and all section pages with empty states.</what-built>
  <how-to-verify>
    1. Run `cd /Users/mjcmatos/Desktop/mytaya && npm run dev` to start dev server
    2. Visit http://localhost:4321/ -- should redirect to /tl/ (Filipino)
    3. On the Filipino homepage:
       a. Check visual identity: colors, typography, logo -- should feel Philippine/casual, NOT like betpro.cl
       b. Verify hero section has headline, subtitle, and two CTA buttons
       c. Verify sport tabs show Basketball | Football | Boxing
       d. Verify empty state placeholders for predictions, results, blog, operators
       e. Verify Telegram CTA banner is prominent
    4. Click the flag icon in header to switch to English (/en/)
       a. Verify all text switches to English
       b. Verify URL changes to /en/
       c. Click flag again to return to /tl/
    5. Click the theme toggle (sun/moon icon):
       a. Should switch from dark to light mode
       b. Refresh the page -- should stay in light mode (localStorage persistence)
       c. Switch back to dark
    6. Navigate to each section page via header nav:
       a. Predictions (/tl/hula/ or /en/predictions/) -- shows empty state
       b. Results (/tl/resulta/ or /en/results/) -- shows empty state
       c. Blog (/tl/blog/ or /en/blog/) -- shows empty state
       d. Operators (/tl/mga-operator/ or /en/operators/) -- shows empty state
    7. Check responsive design: resize browser to mobile width
       a. Hamburger menu appears
       b. Tap hamburger -- menu slides open with nav links
       c. Layout adjusts properly
    8. View page source on any page:
       a. Verify hreflang tags present (fil, en, x-default)
       b. Verify OG meta tags present
    9. Check PWA: in Chrome DevTools > Application tab, verify manifest loads
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds and generates pages for both locales
2. Homepage has all sections in correct order per user decision
3. All 8 sub-pages render with appropriate empty states
4. Language switching works between all pages
5. Theme toggle works with persistence
6. PWA manifest loads correctly
7. Visual identity is distinctly Philippine-inspired
</verification>

<success_criteria>
- User can browse the complete site in both Filipino and English
- Visual identity is clearly distinct from betpro.cl -- fresh, Philippine-inspired, casual brand feel
- Dark theme is default, light theme accessible via toggle with persistence
- All section pages exist as empty-state placeholders ready for future phases
- Site passes human visual/functional inspection
- PWA is installable from browser
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
