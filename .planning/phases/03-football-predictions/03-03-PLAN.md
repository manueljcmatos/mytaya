---
phase: 03-football-predictions
plan: 03
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - src/pages/tl/hula/[slug].astro
  - src/pages/en/predictions/[slug].astro
  - src/components/predictions/PredictionDetail.tsx
  - src/i18n/routes.ts
autonomous: true
requirements:
  - FTBL-05

must_haves:
  truths:
    - "User can visit /tl/hula/[slug] and see a full prediction detail page in Filipino"
    - "User can visit /en/predictions/[slug] and see the same prediction in English"
    - "Detail page shows teams, league, match time, odds, pick, and 2-3 paragraph analysis"
    - "Resolved predictions show final score and WIN/LOSS outcome on the same URL"
    - "Detail page includes Schema.org SportsEvent JSON-LD for SEO"
    - "Language switcher correctly links between /tl/hula/[slug] and /en/predictions/[slug]"
  artifacts:
    - path: "src/components/predictions/PredictionDetail.tsx"
      provides: "React island for individual prediction detail with full analysis"
      min_lines: 60
    - path: "src/pages/tl/hula/[slug].astro"
      provides: "Filipino prediction detail page"
      contains: "PredictionDetail"
    - path: "src/pages/en/predictions/[slug].astro"
      provides: "English prediction detail page"
      contains: "PredictionDetail"
  key_links:
    - from: "src/components/predictions/PredictionDetail.tsx"
      to: "Supabase predictions table"
      via: "client-side fetch by slug"
      pattern: "eq.*slug"
    - from: "src/pages/en/predictions/[slug].astro"
      to: "src/lib/seo.ts"
      via: "buildSportsEventSchema for JSON-LD"
      pattern: "buildSportsEventSchema"
    - from: "src/i18n/routes.ts"
      to: "Prediction slug pages"
      via: "getAlternatePath maps /hula/ to /predictions/"
      pattern: "hula.*predictions"
---

<objective>
Build individual prediction detail pages with bilingual analysis, SportsEvent SEO schema, and proper i18n slug routing. Each prediction gets its own URL that persists through resolution with updated WIN/LOSS status.

Purpose: Detail pages are the SEO-indexed content that drives organic traffic. Each prediction page has unique bilingual analysis, proper schema markup, and a permanent URL that updates in-place when results come in.
Output: Dynamic [slug] pages in both languages with full prediction details and SEO metadata.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-football-predictions/03-CONTEXT.md
@.planning/phases/03-football-predictions/03-RESEARCH.md
@.planning/phases/03-football-predictions/03-02-SUMMARY.md
@src/lib/seo.ts
@src/lib/predictions.ts
@src/lib/supabase.ts
@src/i18n/routes.ts
@src/i18n/ui.ts
@src/components/predictions/PredictionList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PredictionDetail React island for individual prediction pages</name>
  <files>
    src/components/predictions/PredictionDetail.tsx
  </files>
  <action>
    Create a React island component that fetches and displays a single prediction by slug from Supabase.

    **PredictionDetail.tsx**:
    - Props: slug (string), lang ('tl' | 'en').
    - On mount: fetch prediction from Supabase by slug using inline createClient (same pattern as PredictionList -- cannot import Astro's supabase.ts in React island).
    - Query: `.from('predictions').select('*').eq('slug', slug).single()`.
    - Display layout (single column, readable width):
      a. **Header**: League name badge, match date/time in PHT (Intl.DateTimeFormat with Asia/Manila timezone).
      b. **Match info**: Home team vs Away team (from pick_label_{lang} -- extract team names or display the full label). Text-only, no logos per user decision.
      c. **Prediction box**: Highlighted section with:
         - Pick type label (e.g., "Match Winner", "Over/Under", "BTTS")
         - Pick label in current language (pick_label_tl or pick_label_en)
         - Odds value with label
         - Confidence indicator (high/medium/low with color coding)
         - Stake indicator (1-10 scale)
      d. **Result section** (only if status='settled'):
         - Large WIN/LOSS/PUSH badge (same color scheme as ResultBadge but larger)
         - "Resolved" timestamp from settled_at
      e. **Analysis**: Full analysis text (analysis_tl or analysis_en based on lang). Render as prose with proper paragraph breaks. Use Tailwind Typography plugin classes if available, or manual prose styling.
      f. **Back link**: Link back to predictions listing page.

    - Loading state: skeleton layout matching the structure.
    - Not found state: "Prediction not found" message with link back.
    - Hardcoded bilingual translations for all labels (match winner, over/under, btts, analysis, back to predictions, etc.).
    - SportsEvent JSON-LD: Inject a script tag with JSON-LD into the document head using useEffect. Build the schema object matching the shape from src/lib/seo.ts buildSportsEventSchema pattern:
      ```
      { @context, @type: SportsEvent, name: "Home vs Away", startDate, sport: "Football", homeTeam: { @type: SportsTeam, name }, awayTeam: { @type: SportsTeam, name }, url: window.location.href }
      ```
      This client-side injection works because search engines render JS. Alternatively, the Astro wrapper can inject static schema if getStaticPaths is used.

    - Theme compatibility: Use CSS custom properties (var(--t-bg), var(--t-text), etc.) or Tailwind dark: classes matching existing site patterns.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | tail -20</automated>
    <manual>Verify PredictionDetail.tsx renders prediction box, analysis, and result section</manual>
  </verify>
  <done>PredictionDetail.tsx fetches prediction by slug, displays match header, prediction box with pick/odds/confidence, full bilingual analysis, and WIN/LOSS result section. Includes client-side SportsEvent JSON-LD injection.</done>
</task>

<task type="auto">
  <name>Task 2: Create [slug] Astro pages and update i18n route mapping</name>
  <files>
    src/pages/tl/hula/[slug].astro
    src/pages/en/predictions/[slug].astro
    src/i18n/routes.ts
  </files>
  <action>
    1. **src/pages/tl/hula/[slug].astro**:
       - Astro page with [slug] dynamic parameter.
       - Use getStaticPaths that queries Supabase for all published football prediction slugs. Guard with `if (!supabase) return []` for builds without env vars.
       - Import PredictionDetail from components/predictions/PredictionDetail.tsx.
       - Render: `<PredictionDetail client:load slug={slug} lang="tl" />`.
       - Set page title dynamically: fetch prediction in frontmatter to get pick_label_tl for the title tag. If not found, use generic title.
       - Meta description: First 160 chars of analysis_tl.
       - OG tags: title, description, url.
       - Hreflang: link to /en/predictions/{slug} alternate.
       - Optionally inject SportsEvent JSON-LD from Astro frontmatter (server-side) using buildSportsEventSchema from src/lib/seo.ts for better SEO (in addition to client-side injection from the React island -- having both is fine, search engines deduplicate).

    2. **src/pages/en/predictions/[slug].astro**:
       - Same structure as tl version but lang="en".
       - Title from pick_label_en, description from analysis_en.
       - Hreflang links to /tl/hula/{slug} alternate.

    3. **src/i18n/routes.ts**:
       - The existing route map already handles prefix mapping (/hula/ <-> /predictions/) via the prefix match logic in getAlternatePath. Verify this works for slug subpaths: /hula/some-slug/ should map to /predictions/some-slug/.
       - If the prefix match logic doesn't handle this correctly, update the route map entries. The current code iterates entries and uses `startsWith(from)` which should work since /hula/ maps to /predictions/ and /hula/some-slug/ starts with /hula/.
       - No changes needed if prefix matching already works. Add a comment noting that slug subpaths are handled by prefix matching.

    Both pages follow the existing pattern from Phase 1 section pages (Layout wrapper, frontmatter data fetching, meta tags).
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | tail -20</automated>
    <manual>Verify [slug].astro pages have getStaticPaths, PredictionDetail island, and hreflang tags pointing to the alternate language version</manual>
  </verify>
  <done>[slug] pages exist for both /tl/hula/ and /en/predictions/ paths. getStaticPaths queries Supabase for published prediction slugs. PredictionDetail renders with client:load. Pages have proper title, meta description, OG tags, hreflang alternates, and SportsEvent JSON-LD. Language switcher correctly maps between /tl/hula/[slug] and /en/predictions/[slug].</done>
</task>

</tasks>

<verification>
- `npx astro check` passes without errors
- src/pages/tl/hula/[slug].astro and src/pages/en/predictions/[slug].astro both exist with getStaticPaths
- PredictionDetail.tsx renders full prediction with analysis text
- Both [slug] pages include SportsEvent JSON-LD
- Route mapping correctly handles /hula/[slug] <-> /predictions/[slug] for language switching
- Resolved predictions display WIN/LOSS on the same URL (no redirect to separate results page)
</verification>

<success_criteria>
- Individual prediction pages display detailed bilingual analysis (2-3 paragraphs)
- Match details (teams, league, time, odds, pick, confidence) all visible
- Resolved predictions show final WIN/LOSS/PUSH outcome on same URL
- Schema.org SportsEvent JSON-LD present in page source
- Proper SEO meta tags (title, description, OG) with prediction-specific content
- Hreflang tags correctly link /tl/hula/[slug] and /en/predictions/[slug]
- Language switcher works on detail pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-football-predictions/03-03-SUMMARY.md`
</output>
