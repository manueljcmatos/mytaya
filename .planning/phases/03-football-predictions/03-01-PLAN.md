---
phase: 03-football-predictions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/predictions-worker/wrangler.toml
  - workers/predictions-worker/package.json
  - workers/predictions-worker/tsconfig.json
  - workers/predictions-worker/src/index.ts
  - workers/predictions-worker/src/api-football.ts
  - workers/predictions-worker/src/prediction-gen.ts
  - workers/predictions-worker/src/resolver.ts
  - workers/predictions-worker/src/supabase.ts
  - workers/predictions-worker/src/types.ts
  - supabase/migrations/003_seed_leagues.sql
autonomous: true
requirements:
  - FTBL-01
  - FTBL-03
user_setup:
  - service: api-football
    why: "Football match data source"
    env_vars:
      - name: API_FOOTBALL_KEY
        source: "https://dashboard.api-football.com -> My Account -> API Key"
    dashboard_config:
      - task: "Create free account at api-football.com"
        location: "https://www.api-football.com/pricing"
  - service: supabase
    why: "Worker needs service role key for writes"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: SUPABASE_SERVICE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role (secret)"

must_haves:
  truths:
    - "Worker fetches today's fixtures from API-Football for 5 target leagues"
    - "Worker generates bilingual AI predictions with pick, analysis_tl, analysis_en"
    - "Worker stores predictions in Supabase predictions table with correct schema"
    - "Worker resolves finished matches by checking fixture status and updating result/status"
    - "Seed migration inserts football leagues with api_league_id for target leagues"
  artifacts:
    - path: "workers/predictions-worker/src/index.ts"
      provides: "Scheduled handler with two cron patterns"
      contains: "scheduled"
    - path: "workers/predictions-worker/src/api-football.ts"
      provides: "API-Football client for fixtures, odds, results"
      exports: ["fetchTodayFixtures", "fetchOdds", "fetchFixtureResult"]
    - path: "workers/predictions-worker/src/prediction-gen.ts"
      provides: "AI bilingual prediction generation"
      exports: ["generatePrediction"]
    - path: "workers/predictions-worker/src/resolver.ts"
      provides: "Match result resolution logic"
      exports: ["resolveFinishedMatches"]
    - path: "workers/predictions-worker/wrangler.toml"
      provides: "Cron triggers configuration"
      contains: "crons"
    - path: "supabase/migrations/003_seed_leagues.sql"
      provides: "Football leagues seed data"
      contains: "INSERT INTO public.leagues"
  key_links:
    - from: "workers/predictions-worker/src/index.ts"
      to: "workers/predictions-worker/src/api-football.ts"
      via: "import fetchTodayFixtures"
      pattern: "fetchTodayFixtures"
    - from: "workers/predictions-worker/src/index.ts"
      to: "workers/predictions-worker/src/resolver.ts"
      via: "import resolveFinishedMatches"
      pattern: "resolveFinishedMatches"
    - from: "workers/predictions-worker/src/index.ts"
      to: "Supabase predictions table"
      via: "supabase.from('predictions').insert"
      pattern: "predictions.*insert"
---

<objective>
Build the Cloudflare Worker data pipeline that automatically fetches daily football matches from API-Football, generates bilingual AI predictions using Workers AI, stores them in Supabase, and resolves match results when games finish.

Purpose: This is the automated data engine that powers the entire football predictions feature. Without it, there are no predictions to display.
Output: A deployable Cloudflare Worker with cron triggers + a seed migration for football leagues.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-football-predictions/03-CONTEXT.md
@.planning/phases/03-football-predictions/03-RESEARCH.md
@supabase/migrations/00000000000000_foundation.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cloudflare Worker project with API-Football client and prediction generator</name>
  <files>
    workers/predictions-worker/wrangler.toml
    workers/predictions-worker/package.json
    workers/predictions-worker/tsconfig.json
    workers/predictions-worker/src/types.ts
    workers/predictions-worker/src/supabase.ts
    workers/predictions-worker/src/api-football.ts
    workers/predictions-worker/src/prediction-gen.ts
  </files>
  <action>
    Create the Worker project structure from scratch (do NOT use `npm create cloudflare` -- scaffold manually for speed):

    1. **package.json**: Name "predictions-worker", dependencies: @supabase/supabase-js. DevDeps: wrangler, @cloudflare/workers-types, typescript.

    2. **wrangler.toml**: Configure name "mytaya-predictions", compatibility_date, two cron triggers: "0 6 * * *" (daily fetch at 06:00 UTC = 14:00 PHT) and "*/30 * * * *" (resolve every 30 min). Bind Workers AI as `[ai]`. Define env vars: SUPABASE_URL. Secrets (documented in comments): SUPABASE_SERVICE_KEY, API_FOOTBALL_KEY.

    3. **tsconfig.json**: Target ESNext, module ESNext, types from @cloudflare/workers-types.

    4. **src/types.ts**: Define Env interface with bindings (SUPABASE_URL, SUPABASE_SERVICE_KEY, API_FOOTBALL_KEY, AI). Define ApiFootballFixture interface matching API-Football v3 response. Define MatchContext for prediction generation. Define PredictionInsert matching Supabase predictions table columns.

    5. **src/supabase.ts**: Export createSupabaseClient(env) using service role key (NOT anon key -- Worker needs write access).

    6. **src/api-football.ts**:
       - TARGET_LEAGUES constant: Premier League (39), La Liga (140), Serie A (135), Bundesliga (78), Ligue 1 (61) with 2025 season.
       - fetchTodayFixtures(apiKey, date): Fetch fixtures for all target leagues on given date. Add 1100ms delay between league calls for rate limiting on free tier.
       - fetchOdds(apiKey, fixtureId): Fetch odds for a single fixture, return first bookmaker's odds.
       - fetchFixtureResult(apiKey, fixtureId): Fetch single fixture result.
       - All functions use 'x-apisports-key' header and base URL 'https://v3.football.api-sports.io'.
       - Add timezone=Asia/Manila parameter to fixture requests.

    7. **src/prediction-gen.ts**:
       - generatePrediction(ai, match: MatchContext): Use Workers AI binding with @cf/meta/llama-3.1-8b-instruct model.
       - Prompt asks for JSON response with: pick (one of home/away/draw/over/under/btts_yes/btts_no), pick_label_en, pick_label_tl, analysis_en (2-3 paragraphs), analysis_tl (2-3 paragraphs).
       - Parse JSON response with try/catch and fallback for malformed AI output.
       - Prediction types limited to: Match Winner (1X2), Over/Under goals, Both Teams to Score -- per user decision.

    Run `cd workers/predictions-worker && npm install` after creating files.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya/workers/predictions-worker && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify wrangler.toml has two cron patterns and AI binding</manual>
  </verify>
  <done>Worker project compiles with zero TypeScript errors. API-Football client has fetchTodayFixtures, fetchOdds, fetchFixtureResult. Prediction generator uses Workers AI with bilingual prompt. Wrangler.toml configured with crons and bindings.</done>
</task>

<task type="auto">
  <name>Task 2: Create resolver, main scheduled handler, and seed migration</name>
  <files>
    workers/predictions-worker/src/resolver.ts
    workers/predictions-worker/src/index.ts
    supabase/migrations/003_seed_leagues.sql
  </files>
  <action>
    1. **src/resolver.ts**:
       - resolveFinishedMatches(env): Query Supabase for pending football predictions with non-null api_fixture_id. For each, call fetchFixtureResult. If fixture status is 'FT', determine result using determineResult() and update prediction with result, status='settled', settled_at=now.
       - determineResult(pick, homeGoals, awayGoals): Switch on pick type -- home: home>away=win, away: away>home=win, draw: equal=win, over: total>2.5=win, under: total<2.5=win, btts_yes: both>0=win, btts_no: either=0=win. Default: 'void'.
       - Add 1100ms delay between API calls for rate limiting.

    2. **src/index.ts**:
       - Export default scheduled handler that dispatches based on controller.cron pattern.
       - "0 6 * * *" triggers fetchAndGeneratePredictions:
         a. Get today's date in Asia/Manila timezone.
         b. Call fetchTodayFixtures for all target leagues.
         c. Select 3-5 best fixtures (prefer matches with available odds, spread across leagues).
         d. For each selected fixture: fetchOdds, generatePrediction via AI, build slug (league-home-vs-away-date format per research pitfall #4), insert into Supabase predictions table with all fields (slug, league_id looked up by api_league_id, home_team_id/away_team_id upserted by api_team_id, match_date, sport='football', pick, pick_label_tl/en, analysis_tl/en, odds from bookmaker, confidence, stake, published_site=true, api_fixture_id).
         e. Teams: upsert teams by api_team_id (insert if not exists, return id). Use Supabase upsert with onConflict on api_team_id (need to add unique constraint or handle via select-then-insert pattern since teams table uses slug as unique, not api_team_id).
         f. Log summary of predictions created.
       - "*/30 * * * *" triggers resolveFinishedMatches.
       - Wrap all operations in try/catch with console.error logging.

    3. **supabase/migrations/003_seed_leagues.sql**:
       - Insert 5 football leagues matching TARGET_LEAGUES: Premier League (England, api_league_id=39), La Liga (Spain, 140), Serie A (Italy, 135), Bundesliga (Germany, 78), Ligue 1 (France, 61).
       - Use sport='football', api_source='api-sports', is_active=true.
       - Generate deterministic slugs: premier-league, la-liga, serie-a, bundesliga, ligue-1.
       - Use ON CONFLICT (slug) DO NOTHING for idempotency.
       - Also add unique index on teams(api_team_id) WHERE api_team_id IS NOT NULL for Worker upsert pattern.

    Run TypeScript check after completing.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya/workers/predictions-worker && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Review index.ts scheduled handler dispatches correctly on both cron patterns. Check 003_seed_leagues.sql has all 5 leagues.</manual>
  </verify>
  <done>Worker compiles cleanly. Scheduled handler dispatches fetch+predict on daily cron and resolve on 30-min cron. Resolver determines win/loss correctly for all 7 pick types. Seed migration inserts 5 leagues with correct api_league_id values and adds teams api_team_id unique index.</done>
</task>

</tasks>

<verification>
- `cd workers/predictions-worker && npx tsc --noEmit` passes with zero errors
- wrangler.toml contains two cron patterns and AI binding
- src/index.ts exports a scheduled handler
- src/api-football.ts exports fetchTodayFixtures, fetchOdds, fetchFixtureResult
- src/prediction-gen.ts exports generatePrediction
- src/resolver.ts exports resolveFinishedMatches and determineResult
- supabase/migrations/003_seed_leagues.sql inserts 5 leagues with correct api_league_ids
</verification>

<success_criteria>
- Worker project builds without TypeScript errors
- Two cron triggers configured: daily fetch (06:00 UTC) and 30-min resolution
- API-Football client handles fixtures, odds, and results with rate limiting
- AI prediction generator produces bilingual picks with 7 pick types
- Resolver correctly determines win/loss for all pick types
- Seed migration provides 5 major football leagues with API IDs
</success_criteria>

<output>
After completion, create `.planning/phases/03-football-predictions/03-01-SUMMARY.md`
</output>
