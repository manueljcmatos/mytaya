---
phase: 03-football-predictions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/predictions.ts
  - src/i18n/ui.ts
  - src/components/predictions/PredictionCard.astro
  - src/components/predictions/PredictionList.tsx
  - src/components/predictions/ResultBadge.astro
  - src/components/predictions/LeagueFilter.tsx
  - src/pages/tl/hula/index.astro
  - src/pages/en/predictions/index.astro
  - src/pages/tl/resulta/index.astro
  - src/pages/en/results/index.astro
autonomous: true
requirements:
  - FTBL-02
  - FTBL-04

must_haves:
  truths:
    - "User sees today's pending football predictions with teams, league, time, odds, and pick"
    - "User can switch between Today's Picks and Past Results tabs"
    - "User can filter predictions by league using horizontal chips"
    - "Past results show WIN/LOSS badges with color-coded visual treatment"
    - "Predictions are grouped by date with section headers"
    - "Match times display in Philippine Time (Asia/Manila)"
  artifacts:
    - path: "src/lib/predictions.ts"
      provides: "Supabase query helpers and date formatting"
      exports: ["getTodayPredictions", "getPastPredictions", "formatMatchTime", "formatMatchDate"]
    - path: "src/components/predictions/PredictionList.tsx"
      provides: "React island with tab switching, league filter, and prediction rendering"
      min_lines: 80
    - path: "src/components/predictions/PredictionCard.astro"
      provides: "Static prediction card component for SSG contexts"
      min_lines: 20
    - path: "src/components/predictions/ResultBadge.astro"
      provides: "WIN/LOSS/PUSH visual badge"
      min_lines: 10
    - path: "src/pages/tl/hula/index.astro"
      provides: "Filipino predictions listing page"
      contains: "PredictionList"
    - path: "src/pages/en/predictions/index.astro"
      provides: "English predictions listing page"
      contains: "PredictionList"
  key_links:
    - from: "src/components/predictions/PredictionList.tsx"
      to: "Supabase predictions table"
      via: "client-side fetch with createClient"
      pattern: "from.*predictions"
    - from: "src/pages/tl/hula/index.astro"
      to: "src/components/predictions/PredictionList.tsx"
      via: "client:load React island"
      pattern: "client:load"
    - from: "src/pages/en/results/index.astro"
      to: "src/components/predictions/PredictionList.tsx"
      via: "client:load with initialTab=past"
      pattern: "initialTab.*past"
---

<objective>
Build the frontend prediction listing pages as React islands that fetch from Supabase client-side, with tab navigation (Today's Picks / Past Results), league filter chips, and bilingual prediction cards showing match details, odds, and win/loss results.

Purpose: This gives users the core browsing experience -- seeing daily picks and tracking past results -- which is the primary value proposition of the site.
Output: Working predictions and results pages in both Filipino and English, powered by client-side Supabase queries.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-football-predictions/03-CONTEXT.md
@.planning/phases/03-football-predictions/03-RESEARCH.md
@src/lib/supabase.ts
@src/i18n/ui.ts
@src/i18n/routes.ts
@src/components/EmptyState.astro
@src/components/LeadCaptureForm.tsx
@supabase/migrations/00000000000000_foundation.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prediction query helpers, i18n keys, and reusable components</name>
  <files>
    src/lib/predictions.ts
    src/i18n/ui.ts
    src/components/predictions/PredictionCard.astro
    src/components/predictions/ResultBadge.astro
  </files>
  <action>
    1. **src/lib/predictions.ts**: Create Supabase query helper functions following the patterns from 03-RESEARCH.md code examples:
       - getTodayPredictions(): Query predictions table for sport='football', published_site=true, status='pending', match_date=today. Select id, slug, pick, pick_label_tl, pick_label_en, analysis_tl, analysis_en, odds, confidence, stake, match_date, status, result. Order by match_date ascending.
       - getPastPredictions(page, perPage): Same fields but status='settled', order match_date descending, use .range() for pagination. Return { data, count } with count: 'exact'.
       - getPredictionBySlug(slug): Single prediction with full fields.
       - formatMatchTime(isoDate, lang): Use Intl.DateTimeFormat with timeZone 'Asia/Manila', hour:numeric, minute:'2-digit', hour12:true. Locale: 'fil-PH' for tl, 'en-PH' for en.
       - formatMatchDate(isoDate, lang): Weekday long, month long, day numeric. Same timezone.
       - getLeagues(): Query leagues table for sport='football', is_active=true. Return id, name, slug.
       - All functions guard with `if (!supabase) return []` (or null/empty as appropriate).
       - NOTE: Do NOT use JOIN syntax for teams -- the existing predictions table has home_team_id/away_team_id as UUID refs but PredictionList.tsx will query them separately or the Worker will denormalize team names. For now, query predictions with a simple select and handle team name display from the pick_label fields which contain team names.

    2. **src/i18n/ui.ts**: Add prediction-specific keys to BOTH tl and en objects:
       - 'predictions.title': 'Mga Hula sa Football' / 'Football Predictions'
       - 'predictions.todayPicks': "Mga Hula Ngayon" / "Today's Picks"
       - 'predictions.pastResults': 'Mga Nakaraang Resulta' / 'Past Results'
       - 'predictions.allLeagues': 'Lahat' / 'All'
       - 'predictions.odds': 'Odds' / 'Odds'
       - 'predictions.pick': 'Hula' / 'Pick'
       - 'predictions.confidence': 'Kumpiyansa' / 'Confidence'
       - 'predictions.win': 'PANALO' / 'WIN'
       - 'predictions.loss': 'TALO' / 'LOSS'
       - 'predictions.push': 'PUSH' / 'PUSH'
       - 'predictions.pending': 'Naghihintay' / 'Pending'
       - 'predictions.noMatches': 'Walang mga laro ngayon' / 'No matches today'
       - 'predictions.noPastResults': 'Wala pang mga resulta' / 'No results yet'
       - 'predictions.viewDetails': 'Tingnan ang Detalye' / 'View Details'
       - 'results.title': 'Mga Resulta ng Football' / 'Football Results'

    3. **src/components/predictions/ResultBadge.astro**: Small badge component.
       - Props: result ('win' | 'loss' | 'push' | 'void' | null), lang ('tl' | 'en').
       - Win: green background (bg-emerald-500/20 text-emerald-400 in dark, bg-emerald-100 text-emerald-700 in light).
       - Loss: red background (similar pattern with red).
       - Push: yellow/amber.
       - Null/pending: gray with "Pending" text.
       - Uses Bebas Neue or bold uppercase text. Small pill shape.

    4. **src/components/predictions/PredictionCard.astro**: Static Astro component for prediction display.
       - Props: prediction object, lang ('tl' | 'en').
       - Layout: Card with subtle border, hover shadow transition.
       - Shows: league name (small text above), match time in PHT (formatMatchTime), team names (pick_label field split or displayed as-is), odds value, prediction pick badge, ResultBadge if settled.
       - Text-only for teams per user decision (no logos/crests).
       - Link to detail page: `/${lang === 'tl' ? 'tl/hula' : 'en/predictions'}/${prediction.slug}`.
       - Use deep teal brand color for accents. Dark/light theme compatible using CSS custom properties.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | tail -20</automated>
    <manual>Verify i18n keys added to both tl and en objects in ui.ts</manual>
  </verify>
  <done>predictions.ts exports all query helpers with null-safe Supabase guards. ui.ts has 15+ prediction i18n keys in both languages. ResultBadge renders color-coded WIN/LOSS/PUSH/Pending. PredictionCard shows match details with theme-compatible styling.</done>
</task>

<task type="auto">
  <name>Task 2: Build PredictionList React island and wire into listing pages</name>
  <files>
    src/components/predictions/LeagueFilter.tsx
    src/components/predictions/PredictionList.tsx
    src/pages/tl/hula/index.astro
    src/pages/en/predictions/index.astro
    src/pages/tl/resulta/index.astro
    src/pages/en/results/index.astro
  </files>
  <action>
    1. **src/components/predictions/LeagueFilter.tsx**: React component for horizontal scrollable league chips.
       - Props: leagues array, selectedLeague (string|null), onSelect callback, lang.
       - "All" chip first (always present), then league chips from data.
       - Horizontal scroll with overflow-x-auto, hide scrollbar. Chip styling: rounded-full px-4 py-1.5, selected chip uses brand teal bg, unselected uses subtle bg.
       - Uses hardcoded translations object for "All" label (can't use Astro i18n in React).

    2. **src/components/predictions/PredictionList.tsx**: Main React island following LeadCaptureForm pattern (inline Supabase client, hardcoded translations).
       - Props: lang ('tl' | 'en'), initialTab ('today' | 'past').
       - State: tab, predictions[], loading, selectedLeague, leagues[], page, totalCount.
       - On mount: fetch leagues from Supabase (sport='football', is_active=true).
       - On tab/league/page change: fetch predictions.
         - tab='today': status='pending', match_date >= today start, sport='football', published_site=true. Order by match_date asc.
         - tab='past': status='settled', sport='football', published_site=true. Order by match_date desc. Paginate with page state (10 per page).
         - If selectedLeague: filter by league_id matching selected.
       - Inline Supabase client creation: `createClient(import.meta.env.PUBLIC_SUPABASE_URL, import.meta.env.PUBLIC_SUPABASE_ANON_KEY)` per existing island pattern.
       - Render: Tab buttons (Today's Picks / Past Results), LeagueFilter, prediction cards, pagination for past tab.
       - Prediction card inline in this component (not using Astro PredictionCard -- that's for static contexts). Card shows:
         - League name (small caps or muted text)
         - Match time in PHT using Intl.DateTimeFormat('en-PH', { timeZone: 'Asia/Manila', hour: 'numeric', minute: '2-digit', hour12: true })
         - Team display from pick_label_{lang} field
         - Odds badge
         - Pick type badge (e.g., "1X2", "O/U", "BTTS")
         - Result badge if settled (WIN green, LOSS red, PUSH amber)
         - Link to detail page
       - Group predictions by date with section headers ("Today -- March 7", "Yesterday -- March 6", etc.) per user decision.
       - Empty states: "No matches today" or "No results yet" with appropriate messaging per lang.
       - Loading state: skeleton cards or spinner.
       - Hardcoded bilingual translations object for all UI text (tabs, filters, empty states, badges).

    3. **src/pages/tl/hula/index.astro**: Replace EmptyState with PredictionList island.
       - Import PredictionList from components/predictions/PredictionList.tsx.
       - Use `<PredictionList client:load lang="tl" initialTab="today" />`.
       - Page title from i18n: 'predictions.title'.
       - Keep Layout wrapper with proper meta tags.

    4. **src/pages/en/predictions/index.astro**: Same as tl but lang="en".

    5. **src/pages/tl/resulta/index.astro**: Replace EmptyState with PredictionList island.
       - Use `<PredictionList client:load lang="tl" initialTab="past" />`.
       - Page title from i18n: 'results.title'.

    6. **src/pages/en/results/index.astro**: Same as tl resulta but lang="en".
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx astro check 2>&1 | tail -20</automated>
    <manual>Check that all 4 listing pages import PredictionList with correct lang and initialTab props</manual>
  </verify>
  <done>PredictionList React island fetches predictions from Supabase client-side with tab switching, league filtering, date grouping, and pagination. All 4 listing pages (tl/en predictions + results) render the island with correct lang and initialTab props. Empty states show bilingual messages.</done>
</task>

</tasks>

<verification>
- `npx astro check` passes without errors
- src/lib/predictions.ts exports getTodayPredictions, getPastPredictions, getPredictionBySlug, formatMatchTime, formatMatchDate, getLeagues
- src/i18n/ui.ts has prediction keys in both tl and en
- src/components/predictions/PredictionList.tsx creates inline Supabase client and fetches predictions
- All 4 listing pages use PredictionList with client:load directive
- tl/hula and en/predictions use initialTab="today"
- tl/resulta and en/results use initialTab="past"
</verification>

<success_criteria>
- Predictions listing pages display match details (teams, league, time, odds, pick type) for today's pending predictions
- Tab navigation switches between Today's Picks and Past Results
- League filter chips filter predictions by league
- Past results show color-coded WIN/LOSS/PUSH badges
- Predictions grouped by date with section headers
- Match times displayed in Philippine Time
- Both Filipino and English pages work with proper translations
- Empty states shown when no data available
</success_criteria>

<output>
After completion, create `.planning/phases/03-football-predictions/03-02-SUMMARY.md`
</output>
