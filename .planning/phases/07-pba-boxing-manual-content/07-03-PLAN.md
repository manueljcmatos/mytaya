---
phase: 07-pba-boxing-manual-content
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/components/predictions/PredictionList.tsx
autonomous: true
requirements:
  - BASK-04
  - BASK-05
  - BASK-06

must_haves:
  truths:
    - "User can filter basketball predictions by league (NBA, PBA, NCAA PH) on the predictions page"
    - "PBA predictions appear alongside NBA predictions when basketball sport tab is selected"
    - "NCAA Philippines predictions appear alongside NBA predictions when basketball sport tab is selected"
  artifacts:
    - path: "src/components/predictions/PredictionList.tsx"
      provides: "League filtering for basketball showing PBA, NCAA PH alongside NBA"
      contains: "LeagueFilter"
  key_links:
    - from: "src/components/predictions/PredictionList.tsx"
      to: "supabase leagues table"
      via: "Fetches leagues where sport='basketball' and is_active=true"
      pattern: "sport.*basketball"
---

<objective>
Verify and ensure that the existing PredictionList league filtering works with the newly seeded PBA, NCAA PH, and UAAP leagues from Plan 01's migration. The basketball section should show league filter pills for NBA, PBA, NCAA PH, and UAAP when the user selects the basketball sport tab.

Purpose: PBA and NCAA PH predictions are basketball predictions that share infrastructure with NBA. The LeagueFilter already works -- this plan ensures it correctly picks up the new leagues and any edge cases are handled.

Output: Verified and potentially updated PredictionList.tsx that correctly filters by Philippine basketball leagues.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pba-boxing-manual-content/07-RESEARCH.md
@.planning/phases/07-pba-boxing-manual-content/07-01-SUMMARY.md
@src/components/predictions/PredictionList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and update PredictionList league filtering for PBA/NCAA PH</name>
  <files>
    src/components/predictions/PredictionList.tsx
  </files>
  <action>
Read PredictionList.tsx fully to understand how LeagueFilter currently works with the basketball sport tab.

**Verification steps:**
1. Confirm that when sport='basketball' is active, the component fetches leagues from Supabase where sport='basketball' and is_active=true
2. Confirm that LeagueFilter renders a pill/button for each league returned
3. Confirm that selecting a league filters predictions by league_id
4. Confirm that "All" option shows all basketball predictions (NBA + PBA + NCAA PH + UAAP)

**Potential fixes needed (check and apply if necessary):**
- If LeagueFilter only shows when there are 2+ leagues for a sport, it should already work once PBA/NCAA PH leagues are seeded (currently only NBA exists for basketball)
- If the league query has any hardcoded league IDs or names, update to be dynamic
- If predictions query doesn't properly join league data for manual predictions (api_fixture_id IS NULL), ensure the join works for all predictions regardless of source
- Ensure manual predictions (without api_fixture_id) are not accidentally filtered out by any existing query conditions that assume api_fixture_id is non-null
- If the query filters on `api_fixture_id IS NOT NULL` anywhere, remove that filter or make it conditional -- manual predictions for PBA/NCAA PH will have null api_fixture_id

**If PredictionList already handles all the above correctly with no changes needed**, document that in the summary and note that league filtering is verified to work with the new league seeds from Plan 01.

**Additionally**, ensure the hardcoded translations object in PredictionList includes any new labels needed for PBA/NCAA PH display (likely none needed -- league names come from the database, not translations).
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && grep -n "api_fixture_id\|league.*sport\|LeagueFilter\|basketball" src/components/predictions/PredictionList.tsx | head -20 && npx astro check 2>&1 | tail -5</automated>
    <manual>Verify that PredictionList queries don't exclude manual predictions and league filter renders for basketball leagues</manual>
  </verify>
  <done>PredictionList correctly shows league filter pills for all active basketball leagues (NBA, PBA, NCAA PH, UAAP) when basketball sport tab is selected. Manual predictions without api_fixture_id are not filtered out. League filtering by league_id works correctly for all leagues.</done>
</task>

</tasks>

<verification>
- PredictionList.tsx does not filter out manual predictions (api_fixture_id is null)
- League filter query is dynamic (not hardcoded to NBA)
- Basketball sport tab shows all basketball leagues including PBA, NCAA PH, UAAP
- Astro check passes
</verification>

<success_criteria>
- User can filter basketball predictions by league (NBA, PBA, NCAA PH)
- PBA manual predictions display alongside NBA automated predictions
- NCAA Philippines manual predictions display alongside other basketball predictions
- No basketball predictions are accidentally hidden due to null api_fixture_id
</success_criteria>

<output>
After completion, create `.planning/phases/07-pba-boxing-manual-content/07-03-SUMMARY.md`
</output>
