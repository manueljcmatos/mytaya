---
phase: 07-pba-boxing-manual-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/007_boxing_and_ph_leagues.sql
  - workers/predictions-worker/src/types.ts
  - workers/predictions-worker/src/card-templates.ts
  - workers/predictions-worker/src/card-gen.ts
  - workers/predictions-worker/src/resolver.ts
autonomous: true
requirements:
  - BOX-01
  - BOX-02
  - BASK-04
  - BASK-05

must_haves:
  truths:
    - "Boxing predictions can be inserted via Supabase dashboard with fighter records, weight class, and rounds"
    - "PBA, NCAA PH, and UAAP leagues exist in the database for manual basketball prediction entry"
    - "Manual predictions without card_image_url automatically get cards generated by the resolver cron"
    - "Boxing predictions produce a fight-card-style branded PNG card via workers-og"
  artifacts:
    - path: "supabase/migrations/007_boxing_and_ph_leagues.sql"
      provides: "Boxing columns, PH league seeds, PBA/NCAA PH team seeds, auto-resolution trigger"
      contains: "fighter_1_record"
    - path: "workers/predictions-worker/src/types.ts"
      provides: "Boxing sport in type unions"
      contains: "'boxing'"
    - path: "workers/predictions-worker/src/card-templates.ts"
      provides: "Boxing card HTML template"
      contains: "buildBoxingCardHtml"
    - path: "workers/predictions-worker/src/card-gen.ts"
      provides: "Boxing card generation + missing card cron function"
      contains: "generateMissingCards"
  key_links:
    - from: "workers/predictions-worker/src/resolver.ts"
      to: "workers/predictions-worker/src/card-gen.ts"
      via: "generateMissingCards call in resolve cron"
      pattern: "generateMissingCards"
    - from: "workers/predictions-worker/src/card-gen.ts"
      to: "workers/predictions-worker/src/card-templates.ts"
      via: "buildBoxingCardHtml for boxing sport"
      pattern: "buildBoxingCardHtml"
---

<objective>
Add database schema for boxing and Philippine basketball leagues, update worker types to include boxing as a sport, create boxing card template, and add missing-card generation to the resolver cron so manual predictions automatically get branded PNG cards.

Purpose: This plan lays the data foundation for all manual content -- without the migration and type updates, no manual predictions can flow through the existing pipeline (cards, Telegram, blog).

Output: Migration SQL, updated TypeScript types, boxing card template, and generateMissingCards cron function.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pba-boxing-manual-content/07-RESEARCH.md
@workers/predictions-worker/src/types.ts
@workers/predictions-worker/src/card-templates.ts
@workers/predictions-worker/src/card-gen.ts
@workers/predictions-worker/src/resolver.ts
@supabase/migrations/006_telegram_support.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration and worker type updates for boxing and PH leagues</name>
  <files>
    supabase/migrations/007_boxing_and_ph_leagues.sql
    workers/predictions-worker/src/types.ts
  </files>
  <action>
Create migration `007_boxing_and_ph_leagues.sql` with:

1. **Boxing-specific nullable columns on predictions:**
   - `fighter_1_record TEXT` (e.g., "30-2-0 (25 KO)")
   - `fighter_2_record TEXT` (e.g., "28-1-0 (20 KO)")
   - `weight_class TEXT` (e.g., "Welterweight", "Heavyweight")
   - `scheduled_rounds INTEGER` (e.g., 12)
   - Use `ALTER TABLE public.predictions ADD COLUMN IF NOT EXISTS`

2. **Seed Philippine basketball leagues:**
   - PBA: name='PBA', slug='pba', sport='basketball', country='Philippines', api_source='manual', is_active=true
   - NCAA Philippines: name='NCAA Philippines', slug='ncaa-ph', sport='basketball', country='Philippines', api_source='manual', is_active=true
   - UAAP: name='UAAP', slug='uaap', sport='basketball', country='Philippines', api_source='manual', is_active=true
   - Use `ON CONFLICT (slug) DO NOTHING`

3. **Seed Boxing league:**
   - name='Boxing', slug='boxing', sport='boxing', country='International', api_source='manual', is_active=true

4. **Seed PBA teams (12 current teams):**
   - Barangay Ginebra San Miguel, San Miguel Beermen, TNT Tropang Giga, Magnolia Hotshots, Meralco Bolts, Rain or Shine Elasto Painters, NLEX Road Warriors, Phoenix Super LPG Fuel Masters, Blackwater Bossing, Converge FiberXers, Terrafirma Dyip, NorthPort Batang Pier
   - Each team: name, slug (lowercase-hyphenated), sport='basketball'
   - Use `ON CONFLICT (slug) DO NOTHING`

5. **Partial index for boxing queries:**
   - `CREATE INDEX IF NOT EXISTS idx_predictions_boxing ON public.predictions (sport, match_date) WHERE sport = 'boxing'`

6. **PostgreSQL trigger for manual resolution convenience:**
   - Create function `auto_settle_on_result()` that sets `status = 'settled'` and `settled_at = now()` when `result` transitions from NULL to a non-null value
   - Create trigger `trg_auto_settle` on predictions table BEFORE UPDATE

Update `types.ts`:
- `PredictionInsert.sport`: change from `'football' | 'basketball'` to `'football' | 'basketball' | 'boxing'`
- `PredictionInsert`: add optional fields `fighter_1_record?: string`, `fighter_2_record?: string`, `weight_class?: string`, `scheduled_rounds?: number`
- `PredictionInsert.api_fixture_id`: change from `number` to `number | null` (manual predictions have no API fixture)
- `BlogPostInsert.sport`: change from `'football' | 'basketball'` to `'football' | 'basketball' | 'boxing'`
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && cat supabase/migrations/007_boxing_and_ph_leagues.sql | grep -c "fighter_1_record\|fighter_2_record\|weight_class\|scheduled_rounds\|pba\|ncaa-ph\|uaap\|boxing\|auto_settle" && npx tsc --noEmit --project workers/predictions-worker/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify migration contains all boxing columns, league seeds, team seeds, and auto-settle trigger</manual>
  </verify>
  <done>Migration file has boxing columns, 4 league seeds, 12 PBA team seeds, boxing index, and auto-settle trigger. TypeScript types accept 'boxing' sport and boxing-specific nullable fields. Types compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Boxing card template and missing-card generation cron</name>
  <files>
    workers/predictions-worker/src/card-templates.ts
    workers/predictions-worker/src/card-gen.ts
    workers/predictions-worker/src/resolver.ts
  </files>
  <action>
**card-templates.ts updates:**

1. Update `PredictionCardInput.sport` and `ResultCardInput.sport` to include `'boxing'`.
2. Update `getAccentColor` to handle boxing: return `'#DC2626'` (red) for boxing.
3. Add `buildBoxingCardHtml(data: BoxingCardInput): string` function:
   - New interface `BoxingCardInput` extending card data with: fighter1Record, fighter2Record, weightClass, scheduledRounds, plus standard fields (homeTeam as fighter1, awayTeam as fighter2, league, matchDate, pick, odds, confidence)
   - Layout: 1200x630px, dark background (#0A0A0A), red accent (#DC2626)
   - Top bar: MYTAYA wordmark + weight class
   - Center: Fighter 1 name + record on left, "VS" center, Fighter 2 name + record on right
   - Bottom accent bar (red): PICK + odds + confidence badge
   - Footer: mytaya.com + scheduled rounds (e.g., "12 Rounds")
   - All styles inline flexbox (Satori-compatible)

**card-gen.ts updates:**

1. Update `PredictionCardData.sport` and `ResultCardData.sport` to include `'boxing'`.
2. Add boxing-specific fields to `PredictionCardData`: `fighter1Record?: string`, `fighter2Record?: string`, `weightClass?: string`, `scheduledRounds?: number`.
3. In `generatePredictionCard`, add boxing branch: if `data.sport === 'boxing'`, use `buildBoxingCardHtml` instead of `buildPredictionCardHtml`.
4. Add `generateMissingCards(env: Env): Promise<void>` function:
   - Query predictions where `published_site = true AND card_image_url IS NULL`, limit 10
   - Select: slug, sport, match_date, pick_label_en, odds, confidence, fighter_1_record, fighter_2_record, weight_class, scheduled_rounds, plus team and league joins
   - For each prediction, call `generatePredictionCard` with appropriate data
   - Wrap each card generation in try/catch (non-blocking, consistent with existing pattern)

**resolver.ts updates:**

1. Import `generateMissingCards` from `./card-gen`.
2. At the END of the `resolvePendingPredictions` function (or at the start), call `await generateMissingCards(env)` wrapped in try/catch so it never disrupts resolution.
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && npx tsc --noEmit --project workers/predictions-worker/tsconfig.json 2>&1 | head -20 && grep -c "buildBoxingCardHtml\|generateMissingCards" workers/predictions-worker/src/card-templates.ts workers/predictions-worker/src/card-gen.ts workers/predictions-worker/src/resolver.ts</automated>
    <manual>Verify boxing card template produces valid Satori HTML and generateMissingCards queries correct fields</manual>
  </verify>
  <done>Boxing card template renders fight-card-style layout with fighter records, weight class, and red accent. generateMissingCards function picks up manual predictions without cards and generates them. Resolver cron calls generateMissingCards. All worker TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- Migration file exists at supabase/migrations/007_boxing_and_ph_leagues.sql with all required DDL
- Worker TypeScript compiles without errors (npx tsc --noEmit)
- Sport type union includes 'boxing' in all relevant interfaces
- Boxing card template function exists and is called for boxing predictions
- generateMissingCards function exists and is integrated into resolver cron
</verification>

<success_criteria>
- Boxing-specific columns exist in migration (fighter records, weight class, rounds)
- PBA, NCAA PH, UAAP, and Boxing leagues are seeded
- 12 PBA teams are seeded
- Auto-settle trigger simplifies manual resolution to single-field update
- Worker types accept boxing sport throughout (PredictionInsert, BlogPostInsert, card interfaces)
- Boxing predictions get red-accented fight card PNG
- Manual predictions without cards are automatically picked up by cron
</success_criteria>

<output>
After completion, create `.planning/phases/07-pba-boxing-manual-content/07-01-SUMMARY.md`
</output>
