---
phase: 07-pba-boxing-manual-content
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/pages/en/boxing/index.astro
  - src/pages/tl/boksing/index.astro
  - src/pages/en/boxing/[slug].astro
  - src/pages/tl/boksing/[slug].astro
  - src/components/boxing/BoxingList.tsx
  - src/components/boxing/FightCard.tsx
  - src/i18n/routes.ts
  - src/i18n/ui.ts
  - src/lib/constants.ts
  - src/components/SportTabs.astro
autonomous: true
requirements:
  - BOX-01
  - BOX-02
  - BOX-03
  - BOX-04

must_haves:
  truths:
    - "User can navigate to a dedicated boxing section from the sport tabs"
    - "Boxing predictions display in fight card format with fighter profiles, records, weight class, and rounds"
    - "Boxing prediction detail pages show full bilingual analysis with fighter records"
    - "Boxing content is available in both Filipino and English at /en/boxing/ and /tl/boksing/"
  artifacts:
    - path: "src/pages/en/boxing/index.astro"
      provides: "English boxing section landing page"
      min_lines: 20
    - path: "src/pages/tl/boksing/index.astro"
      provides: "Filipino boxing section landing page"
      min_lines: 20
    - path: "src/components/boxing/BoxingList.tsx"
      provides: "Boxing predictions list React island with Supabase fetching"
      min_lines: 80
    - path: "src/components/boxing/FightCard.tsx"
      provides: "Fight card display component with fighter profiles"
      min_lines: 60
    - path: "src/pages/en/boxing/[slug].astro"
      provides: "English boxing detail page"
      min_lines: 30
    - path: "src/pages/tl/boksing/[slug].astro"
      provides: "Filipino boxing detail page"
      min_lines: 30
  key_links:
    - from: "src/components/SportTabs.astro"
      to: "src/pages/en/boxing/index.astro"
      via: "Boxing tab links to /en/boxing/ instead of ?sport=boxing"
      pattern: "/boxing/"
    - from: "src/components/boxing/BoxingList.tsx"
      to: "supabase predictions table"
      via: "Supabase query with sport='boxing'"
      pattern: "sport.*boxing"
    - from: "src/pages/en/boxing/[slug].astro"
      to: "src/components/boxing/FightCard.tsx"
      via: "FightCard rendered on detail page for boxing predictions"
      pattern: "FightCard"
---

<objective>
Build the boxing section with dedicated pages, fight card display component, and bilingual boxing prediction detail pages. Update SportTabs to link boxing to its own section instead of the predictions page with a query parameter.

Purpose: Boxing is fundamentally different from team sports -- fighter vs fighter, weight classes, records, rounds. It needs its own display format (fight card layout) and dedicated section to properly serve Philippine boxing fans.

Output: Boxing section pages (EN + TL), BoxingList island, FightCard component, boxing detail pages, updated routes and SportTabs.
</objective>

<execution_context>
@/Users/mjcmatos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mjcmatos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pba-boxing-manual-content/07-RESEARCH.md
@.planning/phases/07-pba-boxing-manual-content/07-01-SUMMARY.md
@src/components/predictions/PredictionList.tsx
@src/components/predictions/PredictionDetail.tsx
@src/pages/en/predictions/index.astro
@src/components/SportTabs.astro
@src/i18n/routes.ts
@src/i18n/ui.ts
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boxing section pages, BoxingList island, and FightCard component</name>
  <files>
    src/pages/en/boxing/index.astro
    src/pages/tl/boksing/index.astro
    src/components/boxing/BoxingList.tsx
    src/components/boxing/FightCard.tsx
  </files>
  <action>
**FightCard.tsx** -- React component for fight card display:
- Interface `BoxingPrediction` with fields: id, slug, pick_label_tl, pick_label_en, analysis_tl, analysis_en, odds, confidence, match_date, fighter_1_record, fighter_2_record, weight_class, scheduled_rounds, home_team (object with name), away_team (object with name), result, status, card_image_url
- Props: prediction: BoxingPrediction, lang: 'tl' | 'en'
- Layout: Fight card style with red accent (#DC2626) border
  - Header row: weight class left, scheduled rounds right (e.g., "12 Rounds")
  - Center: Fighter 1 name (large) + record below on left, "VS" divider center, Fighter 2 name + record on right
  - Prediction row: Pick label + odds badge + confidence badge
  - If result is set: show result badge (WIN green / LOSS red / PUSH amber) with same colors as existing ResultBadge
  - Optional expand for analysis text (bilingual based on lang prop)
- Use Tailwind classes with CSS custom properties for theme-awareness (same pattern as PredictionList)
- Clickable to navigate to detail page: `/en/boxing/{slug}` or `/tl/boksing/{slug}`

**BoxingList.tsx** -- React island for boxing section:
- Follow PredictionList pattern: inline Supabase client, hardcoded bilingual translations
- Props: lang: 'tl' | 'en', publicUrl: string, publicKey: string
- Fetch predictions from Supabase where sport='boxing', ordered by match_date desc
- Two tabs: "Upcoming" (status='pending', match_date >= now, ordered asc) and "Past" (status='settled', ordered desc)
- Render each prediction as a FightCard
- Handle empty state with bilingual message
- Pagination: load 10 at a time with "Load More" button

**Boxing section pages** (en/boxing/index.astro and tl/boksing/index.astro):
- Follow same pattern as predictions/index.astro
- Layout with boxing-specific heading and description
- Render BoxingList island with `client:load`
- Pass lang, publicUrl (SUPABASE_URL), publicKey (SUPABASE_ANON_KEY) as props
- SEO meta: title "Boxing Predictions | MyTaya" / "Mga Hula sa Boksing | MyTaya"
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && ls -la src/pages/en/boxing/index.astro src/pages/tl/boksing/index.astro src/components/boxing/BoxingList.tsx src/components/boxing/FightCard.tsx && npx astro check 2>&1 | tail -5</automated>
    <manual>Verify fight card layout renders correctly and boxing pages exist in both languages</manual>
  </verify>
  <done>Boxing section pages exist at /en/boxing/ and /tl/boksing/. BoxingList island fetches and displays boxing predictions. FightCard component renders fight card format with fighter profiles, records, weight class, and rounds.</done>
</task>

<task type="auto">
  <name>Task 2: Boxing detail pages, routes, i18n, and SportTabs update</name>
  <files>
    src/pages/en/boxing/[slug].astro
    src/pages/tl/boksing/[slug].astro
    src/i18n/routes.ts
    src/i18n/ui.ts
    src/lib/constants.ts
    src/components/SportTabs.astro
  </files>
  <action>
**Boxing detail pages** ([slug].astro for en and tl):
- Follow PredictionDetail pattern from src/pages/en/predictions/[slug].astro
- Use getStaticPaths to generate paths from Supabase predictions where sport='boxing'
- Render FightCard component for the boxing prediction with full analysis expanded
- Add SportsEvent JSON-LD schema (use existing buildSportsEventSchema with boxing adaptation)
- Custom hreflang tags for /en/boxing/{slug} <-> /tl/boksing/{slug} cross-linking
- OG image from card_image_url if available
- Show bilingual analysis (analysis_tl or analysis_en based on lang)

**i18n/routes.ts updates:**
- Add boxing route mappings: tl: `'/boksing/': '/boxing/'`, en: `'/boxing/': '/boksing/'`
- This enables getAlternatePath to work for language switching on boxing pages

**i18n/ui.ts updates:**
- Add boxing-related i18n keys:
  - `boxing.title`: "Boxing Predictions" / "Mga Hula sa Boksing"
  - `boxing.description`: "Expert boxing predictions and fight analysis" / "Mga ekspertong hula sa boksing at pagsusuri ng laban"
  - `boxing.upcoming`: "Upcoming Fights" / "Mga Paparating na Laban"
  - `boxing.past`: "Past Fights" / "Mga Nakaraang Laban"
  - `boxing.rounds`: "Rounds" / "Rounds"
  - `boxing.record`: "Record" / "Rekord"

**lib/constants.ts updates:**
- Add boxing to NAV_ITEMS for both languages:
  - en: { label: 'Boxing', href: '/en/boxing/' }
  - tl: { label: 'Boksing', href: '/tl/boksing/' }
- Add to SPORTS array if not already present: { id: 'boxing', label: 'Boxing' }

**SportTabs.astro update:**
- Change boxing tab link from `${predictionsBase}?sport=boxing` to a dedicated boxing URL
- Boxing link should be: `lang === 'tl' ? '/tl/boksing/' : '/en/boxing/'`
- Keep basketball and football tabs linking to predictions page with query param (unchanged)
- This fixes Pitfall 3 from research: boxing needs its own page with fight card layout
  </action>
  <verify>
    <automated>cd /Users/mjcmatos/Desktop/mytaya && grep -c "boksing\|boxing" src/i18n/routes.ts src/i18n/ui.ts src/lib/constants.ts src/components/SportTabs.astro && ls -la src/pages/en/boxing/\[slug\].astro src/pages/tl/boksing/\[slug\].astro && npx astro check 2>&1 | tail -5</automated>
    <manual>Verify SportTabs boxing link goes to /en/boxing/ not ?sport=boxing, and detail pages cross-link correctly</manual>
  </verify>
  <done>Boxing detail pages render fight card with full analysis in both languages. SportTabs links boxing to dedicated section. Route mappings enable language switching. Boxing nav items appear in navigation. i18n keys support bilingual boxing content.</done>
</task>

</tasks>

<verification>
- Boxing section accessible at /en/boxing/ and /tl/boksing/
- FightCard component displays fighter names, records, weight class, rounds, pick, odds
- Boxing detail pages have SportsEvent schema and hreflang cross-links
- SportTabs boxing link navigates to /en/boxing/ (not ?sport=boxing)
- Language switcher works on boxing pages
- Astro check passes without errors
</verification>

<success_criteria>
- Dedicated boxing section with fight card display format
- Fighter profiles show records, weight class, and scheduled rounds
- Boxing content available in both Filipino and English
- Detail pages show full analysis with proper SEO metadata
- SportTabs correctly routes to boxing section
</success_criteria>

<output>
After completion, create `.planning/phases/07-pba-boxing-manual-content/07-02-SUMMARY.md`
</output>
